diff --git a/dev/vs/editor/editor.main.css b/dev/vs/editor/editor.main.css
index 417e8ba19145547ea1f35c415c346df9e86534ac..b004b3221c28dea5c5d39212c8d000cedd3f04fe 100644
--- a/dev/vs/editor/editor.main.css
+++ b/dev/vs/editor/editor.main.css
@@ -5343,7 +5343,8 @@
 	overflow: hidden;
 	border-bottom: 1px solid var(--vscode-editorStickyScroll-border);
 	width: 100%;
-	box-shadow: var(--vscode-editorStickyScroll-shadow) 0 4px 2px -2px;
+	/* PATCH: more consistent size sticky shadow through all of its states */
+	box-shadow: var(--vscode-editorStickyScroll-shadow) 0 6px 6px -6px;
 	z-index: 4;
 	right: initial !important;
 	margin-left: '0px';
diff --git a/esm/vs/base/browser/ui/codicons/codicon/codicon-modifiers.css b/esm/vs/base/browser/ui/codicons/codicon/codicon-modifiers.css
index 9666216f6ae00e73fd6ce82d8039f6d97ab179cb..4db2b1f115ade9456fd2e4771ec44914c68e9944 100644
--- a/esm/vs/base/browser/ui/codicons/codicon/codicon-modifiers.css
+++ b/esm/vs/base/browser/ui/codicons/codicon/codicon-modifiers.css
@@ -18,7 +18,7 @@
 .codicon-gear.codicon-modifier-spin,
 .codicon-notebook-state-executing.codicon-modifier-spin {
 	/* Use steps to throttle FPS to reduce CPU usage */
-	animation: codicon-spin 1.5s steps(30) infinite;
+	animation: codicon-spin var(--vscode-animationDuration-ambient, 1.25s) steps(30) infinite;
 }
 
 .codicon-modifier-disabled {
@@ -28,6 +28,6 @@
 /* custom speed & easing for loading icon */
 .codicon-loading,
 .codicon-tree-item-loading::before {
-	animation-duration: 1s !important;
+	animation-duration: var(--vscode-animationDuration-ambient, 1.25s) !important;
 	animation-timing-function: cubic-bezier(0.53, 0.21, 0.29, 0.67) !important;
 }
diff --git a/esm/vs/base/browser/ui/findinput/findInput.css b/esm/vs/base/browser/ui/findinput/findInput.css
index 3de2fd329aca8658d65e4335f171057dd1cc9736..29a79816c3a302196f5568e3ed6a4d07ee260cb7 100644
--- a/esm/vs/base/browser/ui/findinput/findInput.css
+++ b/esm/vs/base/browser/ui/findinput/findInput.css
@@ -31,22 +31,22 @@
 /* Highlighting */
 .monaco-findInput.highlight-0 .controls,
 .hc-light .monaco-findInput.highlight-0 .controls {
-	animation: monaco-findInput-highlight-0 100ms linear 0s;
+	animation: monaco-findInput-highlight-0 var(--vscode-animationDuration-short, 100ms) linear 0s;
 }
 
 .monaco-findInput.highlight-1 .controls,
 .hc-light .monaco-findInput.highlight-1 .controls {
-	animation: monaco-findInput-highlight-1 100ms linear 0s;
+	animation: monaco-findInput-highlight-1 var(--vscode-animationDuration-short, 100ms) linear 0s;
 }
 
 .hc-black .monaco-findInput.highlight-0 .controls,
 .vs-dark  .monaco-findInput.highlight-0 .controls {
-	animation: monaco-findInput-highlight-dark-0 100ms linear 0s;
+	animation: monaco-findInput-highlight-dark-0 var(--vscode-animationDuration-short, 100ms) linear 0s;
 }
 
 .hc-black .monaco-findInput.highlight-1 .controls,
 .vs-dark  .monaco-findInput.highlight-1 .controls {
-	animation: monaco-findInput-highlight-dark-1 100ms linear 0s;
+	animation: monaco-findInput-highlight-dark-1 var(--vscode-animationDuration-short, 100ms) linear 0s;
 }
 
 @keyframes monaco-findInput-highlight-0 {
diff --git a/esm/vs/base/browser/ui/hover/hoverWidget.css b/esm/vs/base/browser/ui/hover/hoverWidget.css
index 11f1fdccc7c09b913394d77b3977d89913a4b516..83750f6f8d6ea076883609accea84cddf0bb9eeb 100644
--- a/esm/vs/base/browser/ui/hover/hoverWidget.css
+++ b/esm/vs/base/browser/ui/hover/hoverWidget.css
@@ -15,7 +15,7 @@
 }
 
 .monaco-hover.fade-in {
-	animation: fadein 100ms linear;
+	animation: fadein var(--vscode-animationDuration-short, 100ms) linear;
 }
 
 .monaco-hover.hidden {
diff --git a/esm/vs/base/browser/ui/menu/menu.js b/esm/vs/base/browser/ui/menu/menu.js
index 424bac8eb56ca35b8754953fe7269cdd408aa2ba..0ce8436d723fdb6e99acb7d5fc4d993cbf35269b 100644
--- a/esm/vs/base/browser/ui/menu/menu.js
+++ b/esm/vs/base/browser/ui/menu/menu.js
@@ -783,7 +783,7 @@ ${formatRule(Codicon.menuSubmenu)}
 .monaco-menu .monaco-action-bar .action-item {
 	cursor: pointer;
 	display: inline-block;
-	transition: transform 50ms ease;
+	transition: transform var(--vscode-animationDuration-instant, 50ms) ease;
 	position: relative;  /* DO NOT REMOVE - this is the key to preventing the ghosting icon bug in Chrome 42 */
 }
 
@@ -968,7 +968,7 @@ ${formatRule(Codicon.menuSubmenu)}
 .context-view.monaco-menu-container {
 	outline: 0;
 	border: none;
-	animation: fadeIn 0.083s linear;
+	animation: fadeIn var(--vscode-animationDuration-short, 100ms) linear;
 	-webkit-app-region: no-drag;
 }
 
@@ -1058,14 +1058,14 @@ ${formatRule(Codicon.menuSubmenu)}
 				/* Background rule added for IE9 - to allow clicks on dom node */
 				background:rgba(0,0,0,0);
 
-				transition: opacity 100ms linear;
+				transition: opacity var(--vscode-animationDuration-short, 100ms) linear;
 			}
 			.monaco-scrollable-element > .invisible {
 				opacity: 0;
 				pointer-events: none;
 			}
 			.monaco-scrollable-element > .invisible.fade {
-				transition: opacity 800ms linear;
+				transition: opacity var(--vscode-animationDuration-slow, 800ms) linear;
 			}
 
 			/* Scrollable Content Inset Shadow */
diff --git a/esm/vs/base/browser/ui/progressbar/progressbar.css b/esm/vs/base/browser/ui/progressbar/progressbar.css
index dc23cd255ecfcf1a6243f8b636d52ce83769f212..db44dbdcdef2965ad55608ce9bfdbffb2e79f8f6 100644
--- a/esm/vs/base/browser/ui/progressbar/progressbar.css
+++ b/esm/vs/base/browser/ui/progressbar/progressbar.css
@@ -23,7 +23,7 @@
 
 .monaco-progress-container.discrete .progress-bit {
 	left: 0;
-	transition: width 100ms linear;
+	transition: width var(--vscode-animationDuration-short, 100ms) linear;
 }
 
 .monaco-progress-container.discrete.done .progress-bit {
@@ -32,7 +32,7 @@
 
 .monaco-progress-container.infinite .progress-bit {
 	animation-name: progress;
-	animation-duration: 4s;
+	animation-duration: var(--vscode-animationDuration-cycle, 4s);
 	animation-iteration-count: infinite;
 	transform: translate3d(0px, 0px, 0px);
 	animation-timing-function: linear;
diff --git a/esm/vs/base/browser/ui/sash/sash.css b/esm/vs/base/browser/ui/sash/sash.css
index 52419ac7d454939c41423b960c0d021cdc3df669..1b8beff5f9efc0f52913e5c21046dc59a71e337c 100644
--- a/esm/vs/base/browser/ui/sash/sash.css
+++ b/esm/vs/base/browser/ui/sash/sash.css
@@ -112,7 +112,7 @@
 }
 
 .monaco-enable-motion .monaco-sash:before {
-	transition: background-color 0.1s ease-out;
+	transition: background-color var(--vscode-animationDuration-short, 100ms) ease-out;
 }
 
 .monaco-sash.hover:before,
diff --git a/esm/vs/base/browser/ui/scrollbar/media/scrollbars.css b/esm/vs/base/browser/ui/scrollbar/media/scrollbars.css
index be500bc2e56c4748b4fd35126495840629c9373b..9e9c07d9805a04483952c0a67ca2155e7f71f7ca 100644
--- a/esm/vs/base/browser/ui/scrollbar/media/scrollbars.css
+++ b/esm/vs/base/browser/ui/scrollbar/media/scrollbars.css
@@ -15,7 +15,7 @@
 	/* Background rule added for IE9 - to allow clicks on dom node */
 	background:rgba(0,0,0,0);
 
-	transition: opacity 100ms linear;
+	transition: opacity var(--vscode-animationDuration-short, 100ms) linear;
 	/* In front of peek view */
 	z-index: 11;
 }
@@ -24,7 +24,7 @@
 	pointer-events: none;
 }
 .monaco-scrollable-element > .invisible.fade {
-	transition: opacity 800ms linear;
+	transition: opacity var(--vscode-animationDuration-slow, 800ms) linear;
 }
 
 /* Scrollable Content Inset Shadow */
diff --git a/esm/vs/base/browser/ui/scrollbar/scrollableElement.js b/esm/vs/base/browser/ui/scrollbar/scrollableElement.js
index 05c1ff7bbbf873939fd7f9eb29bd9958425dd8bc..c84e28fdb07ea79b829f2ceb5fbf5c60fd03fdb0 100644
--- a/esm/vs/base/browser/ui/scrollbar/scrollableElement.js
+++ b/esm/vs/base/browser/ui/scrollbar/scrollableElement.js
@@ -236,6 +236,10 @@ class AbstractScrollableElement extends Widget {
      * Update configuration options for the scrollbar.
      */
     updateOptions(newOptions) {
+        // PATCH: Allow updateOptions() to update alwaysConsumeMouseWheel
+        if (typeof newOptions.alwaysConsumeMouseWheel !== 'undefined') {
+            this._options.alwaysConsumeMouseWheel = newOptions.alwaysConsumeMouseWheel;
+        }
         if (typeof newOptions.handleMouseWheel !== 'undefined') {
             this._options.handleMouseWheel = newOptions.handleMouseWheel;
             this._setListeningToMouseWheel(this._options.handleMouseWheel);
diff --git a/esm/vs/base/browser/ui/table/table.css b/esm/vs/base/browser/ui/table/table.css
index 03dbcb31566ce542bf5f09df063eb72cc6f46373..1ddb1e70ffd459ceba66a03ffea66cfe6be152e6 100644
--- a/esm/vs/base/browser/ui/table/table.css
+++ b/esm/vs/base/browser/ui/table/table.css
@@ -53,5 +53,5 @@
 
 .monaco-enable-motion .monaco-table > .monaco-split-view2,
 .monaco-enable-motion .monaco-table > .monaco-split-view2 .monaco-sash.vertical::before {
-	transition: border-color 0.2s ease-out;
+	transition: border-color var(--vscode-animationDuration-medium, 300ms) ease-out;
 }
diff --git a/esm/vs/base/browser/ui/tree/media/tree.css b/esm/vs/base/browser/ui/tree/media/tree.css
index b5330b90041c2da2342aec46f5103e3901700900..41c1c99393fec2855bad98483f350d74a57a159f 100644
--- a/esm/vs/base/browser/ui/tree/media/tree.css
+++ b/esm/vs/base/browser/ui/tree/media/tree.css
@@ -34,7 +34,7 @@
 }
 
 .monaco-enable-motion .monaco-tl-indent > .indent-guide {
-	transition: opacity 0.1s linear;
+	transition: opacity var(--vscode-animationDuration-short, 100ms) linear;
 }
 
 .monaco-tl-twistie,
@@ -69,7 +69,7 @@
 
 .monaco-tl-twistie.codicon-tree-item-loading::before {
 	/* Use steps to throttle FPS to reduce CPU usage */
-	animation: codicon-spin 1.25s steps(30) infinite;
+	animation: codicon-spin var(--vscode-animationDuration-ambient, 1.25s) steps(30) infinite;
 }
 
 .monaco-tree-type-filter {
@@ -87,7 +87,7 @@
 }
 
 .monaco-enable-motion .monaco-tree-type-filter {
-	transition: top 0.3s;
+	transition: top var(--vscode-animationDuration-medium, 300ms);
 }
 
 .monaco-tree-type-filter.disabled {
diff --git a/esm/vs/base/common/async.js b/esm/vs/base/common/async.js
index 969f181185395a6150f4c44f2d3a3090cb38c5f3..d5de20cf4ad61605f9d2474fa06811c8505ac649 100644
--- a/esm/vs/base/common/async.js
+++ b/esm/vs/base/common/async.js
@@ -248,7 +248,17 @@ class Delayer {
     cancel() {
         this.cancelTimeout();
         if (this.completionPromise) {
-            this.doReject?.(new CancellationError());
+            // PATCH: Remove error throwing during cancellation
+            // The Delayer treats cancellation as an error condition that should be thrown,
+            // but this is problematic during editor disposal where cancellation is a normal
+            // part of the lifecycle. This patch changes the behavior to treat cancellation
+            // as a normal state transition rather than an error condition.
+            //
+            // This fixes unhandled rejections that occur when the editor is disposed while
+            // async operations are pending. The issue affects both test and production
+            // environments.
+            // See: https://github.com/microsoft/monaco-editor/issues/4702
+            // this.doReject?.(new CancellationError());
             this.completionPromise = null;
         }
     }
diff --git a/esm/vs/editor/browser/controller/mouseHandler.js b/esm/vs/editor/browser/controller/mouseHandler.js
index be17f497db57379c0e5f5fc4d87b3a38ffe55a44..68f09c0cc7497c45031a977bd8f9162020b70945 100644
--- a/esm/vs/editor/browser/controller/mouseHandler.js
+++ b/esm/vs/editor/browser/controller/mouseHandler.js
@@ -38,7 +38,10 @@ class MouseHandler extends ViewEventHandler {
             // remove this listener
             if (!this._mouseLeaveMonitor) {
                 this._mouseLeaveMonitor = addDisposableListener(this.viewHelper.viewDomNode.ownerDocument, 'mousemove', (e) => {
-                    if (!this.viewHelper.viewDomNode.contains(e.target)) {
+                    // PATCH: Workaround for Monaco's HoverProvider not working inside a ShadowDOM
+                    // See: https://github.com/microsoft/monaco-editor/issues/3241
+                    // See: https://github.com/microsoft/monaco-editor/issues/3409
+                    if (!this.viewHelper.viewDomNode.contains(e.composedPath()[0])) {
                         // went outside the editor!
                         this._onMouseLeave(new EditorMouseEvent(e, false, this.viewHelper.viewDomNode));
                     }
diff --git a/esm/vs/editor/browser/controller/mouseTarget.js b/esm/vs/editor/browser/controller/mouseTarget.js
index 909381244394b4588c2f6577d5603203c993619b..bbc03b0810e0d0e88b0b22bc1b765acdbbc69378 100644
--- a/esm/vs/editor/browser/controller/mouseTarget.js
+++ b/esm/vs/editor/browser/controller/mouseTarget.js
@@ -891,18 +891,26 @@ class MouseTargetFactory {
         return result;
     }
 }
+function isInlineFolded(el) {
+    return el && el.nodeType === el.ELEMENT_NODE && el.classList.contains('inline-folded');
+}
 function shadowCaretRangeFromPoint(shadowRoot, x, y) {
     const range = document.createRange();
     // Get the element under the point
     // eslint-disable-next-line local/code-no-any-casts, @typescript-eslint/no-explicit-any
     let el = shadowRoot.elementFromPoint(x, y);
+    // PATCH: Fixes for caret range calculation w/folding indicator, a trailing span.inline-folded with no .text
+    if (isInlineFolded(el)) {
+        el = el.parentNode;
+    }
     // When el is not null, it may be div.monaco-mouse-cursor-text Element, which has not childNodes, we don't need to handle it.
     if (el?.hasChildNodes()) {
         // Get the last child of the element until its firstChild is a text node
         // This assumes that the pointer is on the right of the line, out of the tokens
         // and that we want to get the offset of the last token of the line
-        while (el && el.firstChild && el.firstChild.nodeType !== el.firstChild.TEXT_NODE && el.lastChild && el.lastChild.firstChild) {
-            el = el.lastChild;
+        // PATCH: Fixes for caret range calculation w/folding indicator, a trailing span.inline-folded with no .text
+        while (el && el.firstChild && el.firstChild.nodeType !== el.firstChild.TEXT_NODE && el.lastChild && (el.lastChild.firstChild || isInlineFolded(el.lastChild))) {
+            el = isInlineFolded(el.lastChild) ? el.lastChild.previousSibling : el.lastChild;
         }
         // Grab its rect
         const rect = el.getBoundingClientRect();
@@ -944,8 +952,9 @@ function shadowCaretRangeFromPoint(shadowRoot, x, y) {
             }
         }
         // Creates a range with the text node of the element and set the offset found
-        range.setStart(el.firstChild, offset);
-        range.setEnd(el.firstChild, offset);
+        // PATCH: Fix for RTE in shadowCaretRangeFromPoint when selecting lines
+        range.setStart(el.firstChild ?? el, offset);
+        range.setEnd(el.firstChild ?? el, offset);
     }
     return range;
 }
diff --git a/esm/vs/editor/browser/viewParts/editorScrollbar/editorScrollbar.js b/esm/vs/editor/browser/viewParts/editorScrollbar/editorScrollbar.js
index 46c25ca9ffca832406cd14de8ef9d135e42db05f..0e0954e54ab4d65ab128f3e730048f153d1b4397 100644
--- a/esm/vs/editor/browser/viewParts/editorScrollbar/editorScrollbar.js
+++ b/esm/vs/editor/browser/viewParts/editorScrollbar/editorScrollbar.js
@@ -115,6 +115,8 @@ class EditorScrollbar extends ViewPart {
             const fastScrollSensitivity = options.get(49 /* EditorOption.fastScrollSensitivity */);
             const scrollPredominantAxis = options.get(120 /* EditorOption.scrollPredominantAxis */);
             const newOpts = {
+                // PATCH: Allow updateOptions() to update alwaysConsumeMouseWheel
+                alwaysConsumeMouseWheel: scrollbar.alwaysConsumeMouseWheel,
                 vertical: scrollbar.vertical,
                 horizontal: scrollbar.horizontal,
                 verticalScrollbarSize: scrollbar.verticalScrollbarSize,
diff --git a/esm/vs/editor/browser/viewParts/gpuMark/gpuMark.css b/esm/vs/editor/browser/viewParts/gpuMark/gpuMark.css
index 988c4bcb24ad04568f8678468d6d948b5d1a50eb..5892bf5e70c0fc147de00839117a9e188e516d73 100644
--- a/esm/vs/editor/browser/viewParts/gpuMark/gpuMark.css
+++ b/esm/vs/editor/browser/viewParts/gpuMark/gpuMark.css
@@ -12,7 +12,7 @@
 	display: inline-block;
 	border-left: solid 2px var(--vscode-editorWarning-foreground);
 	opacity: 0.2;
-	transition: background-color 0.1s linear;
+	transition: background-color var(--vscode-animationDuration-short, 100ms) linear;
 }
 
 .monaco-editor .margin-view-overlays .gpu-mark:hover {
diff --git a/esm/vs/editor/browser/viewParts/minimap/minimap.css b/esm/vs/editor/browser/viewParts/minimap/minimap.css
index 35bb6e3b7178b3df37b018b6dc629a34076ee085..9d8b485c95ac4a44bb8dae09097190f1b196c4c7 100644
--- a/esm/vs/editor/browser/viewParts/minimap/minimap.css
+++ b/esm/vs/editor/browser/viewParts/minimap/minimap.css
@@ -6,7 +6,7 @@
 /* START cover the case that slider is visible on mouseover */
 .monaco-editor .minimap.slider-mouseover .minimap-slider {
 	opacity: 0;
-	transition: opacity 100ms linear;
+	transition: opacity var(--vscode-animationDuration-short, 100ms) linear;
 }
 .monaco-editor .minimap.slider-mouseover:hover .minimap-slider {
 	opacity: 1;
@@ -47,7 +47,7 @@
 .minimap.minimap-autohide-mouseover,
 .minimap.minimap-autohide-scroll {
 	opacity: 0;
-	transition: opacity 0.5s;
+	transition: opacity var(--vscode-animationDuration-long, 500ms);
 }
 .minimap.minimap-autohide-scroll{
 	pointer-events: none;
diff --git a/esm/vs/editor/browser/viewParts/viewCursors/viewCursors.css b/esm/vs/editor/browser/viewParts/viewCursors/viewCursors.css
index 5549376d99d507c93f0da27de7d852aa85fe4a93..b54a5617183d45d33f62d8fae9b7d957a8d9fa67 100644
--- a/esm/vs/editor/browser/viewParts/viewCursors/viewCursors.css
+++ b/esm/vs/editor/browser/viewParts/viewCursors/viewCursors.css
@@ -15,7 +15,7 @@
 
 /* -- smooth-caret-animation -- */
 .monaco-editor .cursors-layer.cursor-smooth-caret-animation > .cursor {
-	transition: all 80ms;
+	transition: all var(--vscode-animationDuration-short, 100ms);
 }
 
 /* -- block-outline-style -- */
@@ -73,13 +73,13 @@
 }
 
 .cursor-smooth {
-	animation: monaco-cursor-smooth 0.5s ease-in-out 0s 20 alternate;
+	animation: monaco-cursor-smooth var(--vscode-animationDuration-long, 500ms) ease-in-out 0s 20 alternate;
 }
 
 .cursor-phase {
-	animation: monaco-cursor-phase 0.5s ease-in-out 0s 20 alternate;
+	animation: monaco-cursor-phase var(--vscode-animationDuration-long, 500ms) ease-in-out 0s 20 alternate;
 }
 
 .cursor-expand > .cursor {
-	animation: monaco-cursor-expand 0.5s ease-in-out 0s 20 alternate;
+	animation: monaco-cursor-expand var(--vscode-animationDuration-long, 500ms) ease-in-out 0s 20 alternate;
 }
diff --git a/esm/vs/editor/browser/viewParts/viewLines/viewLines.css b/esm/vs/editor/browser/viewParts/viewLines/viewLines.css
index 2e2e717307bf2e7891bec2a949cb8171783667ea..8c1b262ee60e3d10ab76d1bea8f25625ab72a498 100644
--- a/esm/vs/editor/browser/viewParts/viewLines/viewLines.css
+++ b/esm/vs/editor/browser/viewParts/viewLines/viewLines.css
@@ -7,7 +7,7 @@
 /*.monaco-editor .view-lines > .view-line {
 	background-color: none;
 	animation-name: flash-background;
-	animation-duration: 800ms;
+	animation-duration: var(--vscode-animationDuration-slow, 800ms);
 }
 @keyframes flash-background {
 	0%   { background-color: lightgreen; }
diff --git a/esm/vs/editor/browser/widget/codeEditor/editor.css b/esm/vs/editor/browser/widget/codeEditor/editor.css
index d33122122dedf0c3ef6b1e02355caa16fc2c829c..f578a62e9a6b1a5d28e5c0e0e32c7cdffef5e0f8 100644
--- a/esm/vs/editor/browser/widget/codeEditor/editor.css
+++ b/esm/vs/editor/browser/widget/codeEditor/editor.css
@@ -117,3 +117,14 @@
 	text-decoration: line-through;
 	text-decoration-color: var(--vscode-editor-foreground, inherit);
 }
+
+/* PATCH: Add animation duration CSS properties */
+:root {
+	--vscode-animationDuration-instant: 50ms;
+	--vscode-animationDuration-short: 100ms;
+	--vscode-animationDuration-medium: 300ms;
+	--vscode-animationDuration-long: 500ms;
+	--vscode-animationDuration-slow: 800ms;
+	--vscode-animationDuration-ambient: 1.25s;
+	--vscode-animationDuration-cycle: 4s;	
+}
diff --git a/esm/vs/editor/browser/widget/diffEditor/components/diffEditorEditors.js b/esm/vs/editor/browser/widget/diffEditor/components/diffEditorEditors.js
index 3187a99bd9eea0dab59833a57cd556f99f2c41ae..a963a2a9cb1c53d4f49be71e617780f48552c062 100644
--- a/esm/vs/editor/browser/widget/diffEditor/components/diffEditorEditors.js
+++ b/esm/vs/editor/browser/widget/diffEditor/components/diffEditorEditors.js
@@ -65,11 +65,14 @@ let DiffEditorEditors = class DiffEditorEditors extends Disposable {
                 }
             }
         }, (reader, changeSummary) => {
+            // PATCH: preserve and propagate originalAriaLabel and modifiedAriaLabel IDiffEditorConstructionOptions
+            const originalAriaLabel = this.original.getOption(8 /* EditorOption.ariaLabel */);
+            const modifiedAriaLabel = this.modified.getOption(8 /* EditorOption.ariaLabel */);
             /** @description update editor options */
             _options.editorOptions.read(reader);
             this._options.renderSideBySide.read(reader);
-            this.modified.updateOptions(this._adjustOptionsForRightHandSide(reader, changeSummary));
-            this.original.updateOptions(this._adjustOptionsForLeftHandSide(reader, changeSummary));
+            this.original.updateOptions(this._adjustOptionsForLeftHandSide(reader, { originalAriaLabel, ...changeSummary }));
+            this.modified.updateOptions(this._adjustOptionsForRightHandSide(reader, { modifiedAriaLabel, ...changeSummary }));
         }));
     }
     _createLeftHandSideEditor(options, codeEditorWidgetOptions) {
@@ -155,7 +158,8 @@ let DiffEditorEditors = class DiffEditorEditors extends Disposable {
         clonedOptions.scrollbar = { ...(clonedOptions.scrollbar || {}) };
         clonedOptions.folding = false;
         clonedOptions.codeLens = this._options.diffCodeLens.get();
-        clonedOptions.fixedOverflowWidgets = true;
+        // PATCH: respect explicit options, as fixed overflow does not work in a shadow DOM
+        // clonedOptions.fixedOverflowWidgets = true;
         // Clone minimap options before changing them
         clonedOptions.minimap = { ...(clonedOptions.minimap || {}) };
         clonedOptions.minimap.enabled = false;
diff --git a/esm/vs/editor/browser/widget/diffEditor/components/diffEditorViewZones/inlineDiffDeletedCodeMargin.js b/esm/vs/editor/browser/widget/diffEditor/components/diffEditorViewZones/inlineDiffDeletedCodeMargin.js
index d7d24568eb3a404073eb71a4aedbf79d7576914c..bca64e9a2fc8cd613c3615e89df8a92ff37a1509 100644
--- a/esm/vs/editor/browser/widget/diffEditor/components/diffEditorViewZones/inlineDiffDeletedCodeMargin.js
+++ b/esm/vs/editor/browser/widget/diffEditor/components/diffEditorViewZones/inlineDiffDeletedCodeMargin.js
@@ -40,7 +40,7 @@ class InlineDiffDeletedCodeMargin extends Disposable {
         this._diffActions.className = ThemeIcon.asClassName(Codicon.lightBulb) + ' lightbulb-glyph';
         this._diffActions.style.position = 'absolute';
         const lineHeight = this._modifiedEditor.getOption(75 /* EditorOption.lineHeight */);
-        this._diffActions.style.right = '0px';
+        this._diffActions.style.right = '-2px';
         this._diffActions.style.visibility = 'hidden';
         this._diffActions.style.height = `${lineHeight}px`;
         this._diffActions.style.lineHeight = `${lineHeight}px`;
diff --git a/esm/vs/editor/browser/widget/diffEditor/delegatingEditorImpl.js b/esm/vs/editor/browser/widget/diffEditor/delegatingEditorImpl.js
index 8dd4050bd048abb31f44f69658d4403f91b52d26..b999412a3ff25d7c6882ee8a6701d26a5d290235 100644
--- a/esm/vs/editor/browser/widget/diffEditor/delegatingEditorImpl.js
+++ b/esm/vs/editor/browser/widget/diffEditor/delegatingEditorImpl.js
@@ -106,6 +106,12 @@ class DelegatingEditor extends Disposable {
     changeDecorations(callback) {
         return this._targetEditor.changeDecorations(callback);
     }
+    // PATCH: Fire missing onDidDispose event.
+    // See: https://github.com/microsoft/monaco-editor/issues/4714
+    dispose() {
+        this._onDidDispose.fire();
+        super.dispose();
+    }
 }
 
 export { DelegatingEditor };
diff --git a/esm/vs/editor/browser/widget/diffEditor/features/gutterFeature.js b/esm/vs/editor/browser/widget/diffEditor/features/gutterFeature.js
index 01877be4c38d00326feeb1067c81a440bead31c0..d58aa77a2cef63e76920cd4de12f2fb43acf4c20 100644
--- a/esm/vs/editor/browser/widget/diffEditor/features/gutterFeature.js
+++ b/esm/vs/editor/browser/widget/diffEditor/features/gutterFeature.js
@@ -92,6 +92,10 @@ let DiffEditorGutter = class DiffEditorGutter extends Disposable {
         this._register(addDisposableListener(this.elements.root, 'click', () => {
             this._editors.modified.focus();
         }));
+        // PATCH: fix a bug where the menu is incorrectly parented to the wrong (focused) editor
+        this._register(addDisposableListener(this.elements.root, 'mousedown', () => {
+            this._editors.modified.focus();
+        }, true));
         this._register(applyStyle(this.elements.root, { display: this._hasActions.map(a => a ? 'block' : 'none') }));
         derivedDisposable(this, reader => {
             const showSash = this._showSash.read(reader);
@@ -190,6 +194,8 @@ let DiffToolBar = class DiffToolBar extends Disposable {
                     primaryGroup: g => g.startsWith('primary'),
                 },
                 overflowBehavior: { maxItems: this._isSmall.read(reader) ? 1 : 3 },
+                // PATCH: fix a bug where menu is incorrectly positioned when editor is within a shadow DOM
+                menuAsChild: true,
                 hiddenItemStrategy: 0 /* HiddenItemStrategy.Ignore */,
                 actionRunner: store.add(new ActionRunnerWithContext(() => {
                     const item = this._item.read(undefined);
diff --git a/esm/vs/editor/browser/widget/diffEditor/style.css b/esm/vs/editor/browser/widget/diffEditor/style.css
index 6e27efbe75aeed885fece4ca4ae38e1ff78d74bd..d32f3778e8f6afe9f9a82f8d3d72176aa647223d 100644
--- a/esm/vs/editor/browser/widget/diffEditor/style.css
+++ b/esm/vs/editor/browser/widget/diffEditor/style.css
@@ -23,7 +23,7 @@
 
 .monaco-editor .diff-hidden-lines .top,
 .monaco-editor .diff-hidden-lines .bottom {
-	transition: background-color 0.1s ease-out;
+	transition: background-color var(--vscode-animationDuration-short, 100ms) ease-out;
 	height: 4px;
 	background-color: transparent;
 	background-clip: padding-box;
@@ -338,7 +338,7 @@
 
 	.gutterItem {
 		opacity: 0;
-		transition: opacity 0.7s;
+		transition: opacity var(--vscode-animationDuration-slow, 800ms);
 
 		&.showAlways {
 			opacity: 1;
@@ -352,7 +352,7 @@
 
 	&:hover .gutterItem {
 		opacity: 1;
-		transition: opacity 0.1s ease-in-out;
+		transition: opacity var(--vscode-animationDuration-short, 100ms) ease-in-out;
 	}
 
 	.gutterItem {
diff --git a/esm/vs/editor/browser/widget/multiDiffEditor/diffEditorItemTemplate.js b/esm/vs/editor/browser/widget/multiDiffEditor/diffEditorItemTemplate.js
index d272dc1a9cac2191cbc1f3f6ef73a5742162ad6a..331f6fa53c24df1a3c4c62a4c616f2606e964c76 100644
--- a/esm/vs/editor/browser/widget/multiDiffEditor/diffEditorItemTemplate.js
+++ b/esm/vs/editor/browser/widget/multiDiffEditor/diffEditorItemTemplate.js
@@ -175,7 +175,8 @@ let DiffEditorItemTemplate = class DiffEditorItemTemplate extends Disposable {
                     useShadows: false,
                 },
                 renderOverviewRuler: false,
-                fixedOverflowWidgets: true,
+                // PATCH: respect explicit options, as fixed overflow does not work in a shadow DOM
+                // fixedOverflowWidgets: true,
                 overviewRulerBorder: false,
             };
         }
diff --git a/esm/vs/editor/common/model/textModel.js b/esm/vs/editor/common/model/textModel.js
index 5ea5530740156d0602887b5a72155f654a92e214..3e04aa520519ef8f82c992bfa47aed016d564db0 100644
--- a/esm/vs/editor/common/model/textModel.js
+++ b/esm/vs/editor/common/model/textModel.js
@@ -179,6 +179,9 @@ let TextModel = class TextModel extends Disposable {
         this._languageSelectionListener = this._register(new MutableDisposable());
         this._deltaDecorationCallCnt = 0;
         this._attachedViews = new AttachedViews();
+        // PATCH: Enable workers to communicate that syntax validation is complete for a specific model version
+        this._onDidValidateVersion = new Emitter();
+        this.onDidValidateVersion = this._onDidValidateVersion.event;
         // Generate a new unique model id
         MODEL_ID++;
         this.id = '$model' + MODEL_ID;
@@ -256,6 +259,10 @@ let TextModel = class TextModel extends Disposable {
         this._buffer = emptyDisposedTextBuffer;
         this._bufferDisposable = Disposable.None;
     }
+    // PATCH: Enable workers to communicate that syntax validation is complete for a specific model version
+    setVersionValidated(versionId) {
+        this._onDidValidateVersion.fire(versionId);
+    }
     _assertNotDisposed() {
         if (this._isDisposed) {
             throw new BugIndicatingError('Model is disposed!');
diff --git a/esm/vs/editor/contrib/codelens/browser/codelensWidget.css b/esm/vs/editor/contrib/codelens/browser/codelensWidget.css
index a7ec91c06d96824d0a54f95fa5c3d886e01a874a..5cb31704d917e126557ec6c06f1df0d1d7b9c724 100644
--- a/esm/vs/editor/contrib/codelens/browser/codelensWidget.css
+++ b/esm/vs/editor/contrib/codelens/browser/codelensWidget.css
@@ -59,5 +59,5 @@
 }
 
 .monaco-editor .codelens-decoration.fadein {
-	animation: fadein 0.1s linear;
+	animation: fadein var(--vscode-animationDuration-short, 100ms) linear;
 }
diff --git a/esm/vs/editor/contrib/find/browser/findWidget.css b/esm/vs/editor/contrib/find/browser/findWidget.css
index a8f936985138e8ca882cdb81204b699208d7707c..b0f9e26943dbc4daded7d39e8610cecf4b578d7c 100644
--- a/esm/vs/editor/contrib/find/browser/findWidget.css
+++ b/esm/vs/editor/contrib/find/browser/findWidget.css
@@ -10,7 +10,7 @@
 	height: 33px;
 	overflow: hidden;
 	line-height: 19px;
-	transition: transform 200ms linear;
+	transition: transform var(--vscode-animationDuration-medium, 200ms) linear;
 	padding: 0 4px;
 	box-sizing: border-box;
 	transform: translateY(calc(-100% - 10px)); /* shadow (10px) */
diff --git a/esm/vs/editor/contrib/folding/browser/folding.css b/esm/vs/editor/contrib/folding/browser/folding.css
index 11ee8010be44daf620f027ad67403e1a5ee6b0e5..83bc51f61a60614065add8ab39a698556eeb520a 100644
--- a/esm/vs/editor/contrib/folding/browser/folding.css
+++ b/esm/vs/editor/contrib/folding/browser/folding.css
@@ -8,7 +8,7 @@
 .monaco-editor .margin-view-overlays .codicon-folding-collapsed {
 	cursor: pointer;
 	opacity: 0;
-	transition: opacity 0.5s;
+	transition: opacity var(--vscode-animationDuration-long, 500ms);
 	display: flex;
 	align-items: center;
 	justify-content: center;
diff --git a/esm/vs/editor/contrib/gotoSymbol/browser/peek/referencesWidget.js b/esm/vs/editor/contrib/gotoSymbol/browser/peek/referencesWidget.js
index 14da55aa45ed683df456d75adf74ccb59e1faa25..21dc2e94fe1ca1125b38d5eb51aafdcc1c8f593d 100644
--- a/esm/vs/editor/contrib/gotoSymbol/browser/peek/referencesWidget.js
+++ b/esm/vs/editor/contrib/gotoSymbol/browser/peek/referencesWidget.js
@@ -295,7 +295,8 @@ let ReferenceWidget = class ReferenceWidget extends PeekViewWidget {
                 alwaysConsumeMouseWheel: true
             },
             overviewRulerLanes: 2,
-            fixedOverflowWidgets: true,
+            // PATCH: fixed overflow does not work in a shadow DOM
+            // fixedOverflowWidgets: true,
             minimap: {
                 enabled: false
             }
diff --git a/esm/vs/editor/contrib/inlineCompletions/browser/view/inlineEdits/components/gutterIndicatorView.js b/esm/vs/editor/contrib/inlineCompletions/browser/view/inlineEdits/components/gutterIndicatorView.js
index 4f2b65df75df0de7728ba6ba9a86c924e203b37a..c9f49931360cd062469959c229e286fd0f699b33 100644
--- a/esm/vs/editor/contrib/inlineCompletions/browser/view/inlineEdits/components/gutterIndicatorView.js
+++ b/esm/vs/editor/contrib/inlineCompletions/browser/view/inlineEdits/components/gutterIndicatorView.js
@@ -323,7 +323,7 @@ let InlineEditsGutterIndicator = class InlineEditsGutterIndicator extends Dispos
                     borderRadius: '4px',
                     display: 'flex',
                     justifyContent: 'flex-end',
-                    transition: 'background-color 0.2s ease-in-out, width 0.2s ease-in-out',
+                    transition: 'background-color var(--vscode-animationDuration-medium, 200ms) ease-in-out, width var(--vscode-animationDuration-medium, 200ms) ease-in-out',
                     ...rectToProps(reader => layout.read(reader).pillRect),
                 }
             }, [
@@ -342,7 +342,7 @@ let InlineEditsGutterIndicator = class InlineEditsGutterIndicator extends Dispos
                 n.div({
                     style: {
                         rotate: layout.map(l => `${getRotationFromDirection(l.iconDirection)}deg`),
-                        transition: 'rotate 0.2s ease-in-out',
+                        transition: 'rotate var(--vscode-animationDuration-medium, 200ms) ease-in-out',
                         display: 'flex',
                         alignItems: 'center',
                         justifyContent: 'center',
diff --git a/esm/vs/editor/contrib/inlineCompletions/browser/view/inlineEdits/view.css b/esm/vs/editor/contrib/inlineCompletions/browser/view/inlineEdits/view.css
index f93e486260393a423db91ae2837b2df7f74c26d9..f7215e5069700f36a2e7412c8326f318eea84121 100644
--- a/esm/vs/editor/contrib/inlineCompletions/browser/view/inlineEdits/view.css
+++ b/esm/vs/editor/contrib/inlineCompletions/browser/view/inlineEdits/view.css
@@ -32,8 +32,8 @@
 		opacity: 0;
 
 		&.contained {
-			transition: opacity 0.2s ease-in-out;
-			transition-delay: 0.4s;
+			transition: opacity var(--vscode-animationDuration-medium, 200ms) ease-in-out;
+			transition-delay: var(--vscode-animationDuration-long, 400ms);
 		}
 
 		&.visible {
@@ -61,7 +61,7 @@
 			align-items: center;
 			margin: 0 2px;
 			transform: none;
-			transition: transform 0.2s ease-in-out;
+			transition: transform var(--vscode-animationDuration-medium, 200ms) ease-in-out;
 			.codicon {
 				color: var(--vscode-inlineEdit-gutterIndicator-primaryForeground);
 			}
diff --git a/esm/vs/editor/contrib/message/browser/messageController.css b/esm/vs/editor/contrib/message/browser/messageController.css
index 6ea335781ce0df57917f086974bed027421f8411..9532c077395c83bc959d920bee5beea0a04ca635 100644
--- a/esm/vs/editor/contrib/message/browser/messageController.css
+++ b/esm/vs/editor/contrib/message/browser/messageController.css
@@ -19,7 +19,7 @@
 	to { opacity: 1; }
 }
 .monaco-editor .monaco-editor-overlaymessage.fadeIn {
-	animation: fadeIn 150ms ease-out;
+	animation: fadeIn var(--vscode-animationDuration-medium, 300ms) ease-out;
 }
 
 @keyframes fadeOut {
@@ -27,7 +27,7 @@
 	to { opacity: 0; }
 }
 .monaco-editor .monaco-editor-overlaymessage.fadeOut {
-	animation: fadeOut 100ms ease-out;
+	animation: fadeOut var(--vscode-animationDuration-short, 100ms) ease-out;
 }
 
 .monaco-editor .monaco-editor-overlaymessage .message {
diff --git a/esm/vs/editor/contrib/stickyScroll/browser/stickyScroll.css b/esm/vs/editor/contrib/stickyScroll/browser/stickyScroll.css
index ecc59245decf80102324d31128af25843a108cb9..6e4946bac8cce9632033a4e8efeb10588875f4b2 100644
--- a/esm/vs/editor/contrib/stickyScroll/browser/stickyScroll.css
+++ b/esm/vs/editor/contrib/stickyScroll/browser/stickyScroll.css
@@ -7,7 +7,8 @@
 	overflow: hidden;
 	border-bottom: 1px solid var(--vscode-editorStickyScroll-border);
 	width: 100%;
-	box-shadow: var(--vscode-editorStickyScroll-shadow) 0 4px 2px -2px;
+	/* PATCH: more consistent size sticky shadow through all of its states */
+	box-shadow: var(--vscode-editorStickyScroll-shadow) 0 6px 6px -6px;
 	z-index: 4;
 	right: initial !important;
 	margin-left: '0px';
diff --git a/esm/vs/editor/editor.api.d.ts b/esm/vs/editor/editor.api.d.ts
index afd0fca4a0e8ebaad5f1948d6bbc049432d41af4..78398813a240af6613404d8db2985a0ad7789e63 100644
--- a/esm/vs/editor/editor.api.d.ts
+++ b/esm/vs/editor/editor.api.d.ts
@@ -1033,6 +1033,19 @@ declare namespace editor {
 	 */
 	export function setModelLanguage(model: ITextModel, mimeTypeOrLanguageId: string): void;
 
+	// PATCH: Enable workers to communicate that syntax validation is complete for a specific model version
+	/**
+	 * Set a version of the model as validated.
+	 */
+	export function setModelVersionValidated(model: ITextModel, versionId: number): void;
+
+	// PATCH: Enable workers to communicate that syntax validation is complete for a specific model version
+	/**
+	 * Emitted when a model version has been validated.
+	 * @event
+	 */
+	export function onDidValidateModelVersion(model: ITextModel, listener: (versionId: number) => void): IDisposable;
+
 	/**
 	 * Set the markers for a model.
 	 */
@@ -1127,6 +1140,12 @@ declare namespace editor {
 	 */
 	export function setTheme(themeName: string): void;
 
+	// PATCH: Add missing accessor for current theme.
+	/**
+	 * Get the current theme.
+	 */
+	export function getTheme(): string;
+
 	/**
 	 * Clears all cached font measurements and triggers re-measurement.
 	 */
@@ -2428,6 +2447,18 @@ declare namespace editor {
 		 * Returns if this model is attached to an editor or not.
 		 */
 		isAttachedToEditor(): boolean;
+
+        // PATCH: Enable workers to communicate that syntax validation is complete for a specific model version
+        /**
+         * Set a version of the model as validated.
+         */
+        setVersionValidated(versionId: number): void;
+
+        // PATCH: Enable workers to communicate that syntax validation is complete for a specific model version
+        /**
+         * Register a listener for model version validation events.
+         */
+        onDidValidateVersion(listener: (versionId: number) => void): IDisposable;
 	}
 
 	export enum PositionAffinity {
@@ -4662,10 +4693,10 @@ declare namespace editor {
 		 * Defaults to true.
 		 */
 		handleMouseWheel?: boolean;
+		// PATCH: Allow updateOptions() to update alwaysConsumeMouseWheel
 		/**
 		 * Always consume mouse wheel events (always call preventDefault() and stopPropagation() on the browser events).
 		 * Defaults to true.
-		 * **NOTE**: This option cannot be updated using `updateOptions()`
 		 */
 		alwaysConsumeMouseWheel?: boolean;
 		/**
diff --git a/esm/vs/editor/standalone/browser/standaloneEditor.js b/esm/vs/editor/standalone/browser/standaloneEditor.js
index 9490e25363fb401bfc0dae094012675c1e8a5ba9..adfa224edb41f454732cb16ee66b2998aeb532ec 100644
--- a/esm/vs/editor/standalone/browser/standaloneEditor.js
+++ b/esm/vs/editor/standalone/browser/standaloneEditor.js
@@ -187,6 +187,22 @@ function setModelLanguage(model, mimeTypeOrLanguageId) {
     const languageId = languageService.getLanguageIdByMimeType(mimeTypeOrLanguageId) || mimeTypeOrLanguageId || PLAINTEXT_LANGUAGE_ID;
     model.setLanguage(languageService.createById(languageId));
 }
+// PATCH: Enable workers to communicate that syntax validation is complete for a specific model version
+/**
+ * Set a version of the model as validated.
+ */
+export function setModelVersionValidated(model, versionId) {
+    if (model) {
+        model.setVersionValidated(versionId);
+    }
+}
+/**
+ * Emitted when a model version has been validated.
+ * @event
+ */
+export function onDidValidateModelVersion(model, listener) {
+    return model.onDidValidateModelVersion(listener);
+}
 /**
  * Set the markers for a model.
  */
@@ -335,6 +351,14 @@ function defineTheme(themeName, themeData) {
     const standaloneThemeService = StandaloneServices.get(IStandaloneThemeService);
     standaloneThemeService.defineTheme(themeName, themeData);
 }
+// PATCH: Add missing accessor for current theme.
+/**
+ * Get the current theme.
+ */
+export function getTheme() {
+    const standaloneThemeService = StandaloneServices.get(IStandaloneThemeService);
+    return standaloneThemeService.getColorTheme().themeName;
+}
 /**
  * Switches to a theme.
  */
@@ -429,6 +453,11 @@ function createMonacoEditorAPI() {
         createModel: createModel,
         // eslint-disable-next-line local/code-no-any-casts
         setModelLanguage: setModelLanguage,
+        // PATCH: Enable workers to communicate that syntax validation is complete for a specific model version
+        // eslint-disable-next-line local/code-no-any-casts
+        setModelVersionValidated: setModelVersionValidated,
+        // eslint-disable-next-line local/code-no-any-casts
+        onDidValidateModelVersion: onDidValidateModelVersion,        
         // eslint-disable-next-line local/code-no-any-casts
         setModelMarkers: setModelMarkers,
         // eslint-disable-next-line local/code-no-any-casts
@@ -458,6 +487,9 @@ function createMonacoEditorAPI() {
         tokenize: tokenize,
         // eslint-disable-next-line local/code-no-any-casts
         defineTheme: defineTheme,
+        // PATCH: Add missing accessor for current theme.
+        // eslint-disable-next-line local/code-no-any-casts
+        getTheme: getTheme,
         // eslint-disable-next-line local/code-no-any-casts
         setTheme: setTheme,
         remeasureFonts: remeasureFonts,
diff --git a/esm/vs/language/common/lspLanguageFeatures.js b/esm/vs/language/common/lspLanguageFeatures.js
index 12a49bd4ac441dcbf3db21a25fb15614294f17d8..b5087cf444492a8bcabed2a748efcb9561175cde 100644
--- a/esm/vs/language/common/lspLanguageFeatures.js
+++ b/esm/vs/language/common/lspLanguageFeatures.js
@@ -133,13 +133,23 @@ class DiagnosticsAdapter {
     this._disposables.length = 0;
   }
   _doValidate(resource, languageId) {
+    // PATCH: Enable workers to communicate that syntax validation is complete for a specific model version
+    const model = editor.getModel(resource);
+    if (!model) {
+      return;
+    }
+    const modelVersionId = model.getVersionId();
     this._worker(resource).then((worker) => {
       return worker.doValidation(resource.toString());
     }).then((diagnostics) => {
+      // PATCH: Enable workers to communicate that syntax validation is complete for a specific model version
+      // let model = editor.getModel(resource);
       const markers = diagnostics.map((d) => toDiagnostics(resource, d));
-      let model = editor.getModel(resource);
-      if (model && model.getLanguageId() === languageId) {
+      // PATCH: Gracefully handle the case where the model changed or was disposed during async validation.
+      if (model && !model.isDisposed() && model.getLanguageId() === languageId && model.getVersionId() === modelVersionId) {
         editor.setModelMarkers(model, languageId, markers);
+        // PATCH: Enable workers to communicate that syntax validation is complete for a specific model version
+        editor.setModelVersionValidated(model, modelVersionId);
       }
     }).then(void 0, (err) => {
       console.error(err);
@@ -482,26 +492,11 @@ class DocumentSymbolAdapter {
       if (!items) {
         return;
       }
-      return items.map((item) => {
-        if (isDocumentSymbol(item)) {
-          return toDocumentSymbol(item);
-        }
-        return {
-          name: item.name,
-          detail: "",
-          containerName: item.containerName,
-          kind: toSymbolKind(item.kind),
-          range: toRange(item.location.range),
-          selectionRange: toRange(item.location.range),
-          tags: []
-        };
-      });
+      // PATCH: Ensure the new standard DocumentSymbol data structure is provided from language services
+      return items.map((item) => toDocumentSymbol(item));
     });
   }
 }
-function isDocumentSymbol(symbol) {
-  return "children" in symbol;
-}
 function toDocumentSymbol(symbol) {
   return {
     name: symbol.name,
diff --git a/esm/vs/language/css/cssWorker.js b/esm/vs/language/css/cssWorker.js
index 413afcb2bfe66d80302f27aa02324d865777f851..a3a59caf9930c48dd7be75fcba3be075a3265028 100644
--- a/esm/vs/language/css/cssWorker.js
+++ b/esm/vs/language/css/cssWorker.js
@@ -94,7 +94,8 @@ class CSSWorker {
       return [];
     }
     const stylesheet = this._languageService.parseStylesheet(document);
-    const symbols = this._languageService.findDocumentSymbols(document, stylesheet);
+    // PATCH: Ensure the new standard DocumentSymbol data structure is provided from language services
+    const symbols = this._languageService.findDocumentSymbols2(document, stylesheet);    
     return Promise.resolve(symbols);
   }
   async doCodeActions(uri, range, context) {
diff --git a/esm/vs/language/html/htmlWorker.js b/esm/vs/language/html/htmlWorker.js
index 9e9a1cab25d7c35fb2632587c8ee3dcaac8a73c0..7d2ed929c787e3f8a9a086e9952b6981d2988582 100644
--- a/esm/vs/language/html/htmlWorker.js
+++ b/esm/vs/language/html/htmlWorker.js
@@ -79,7 +79,8 @@ class HTMLWorker {
       return [];
     }
     let htmlDocument = this._languageService.parseHTMLDocument(document);
-    let symbols = this._languageService.findDocumentSymbols(document, htmlDocument);
+    // PATCH: Ensure the new standard DocumentSymbol data structure is provided from language services
+    let symbols = this._languageService.findDocumentSymbols2(document, htmlDocument);
     return Promise.resolve(symbols);
   }
   async getFoldingRanges(uri, context) {
diff --git a/esm/vs/language/typescript/languageFeatures.js b/esm/vs/language/typescript/languageFeatures.js
index 485cbd9239469d5168907e2250d5bf1615116c2b..e5565c067e336f3014c5c3829a1f02a64a54a3b8 100644
--- a/esm/vs/language/typescript/languageFeatures.js
+++ b/esm/vs/language/typescript/languageFeatures.js
@@ -261,6 +261,8 @@ class DiagnosticsAdapter extends Adapter {
     this._disposables = [];
   }
   async _doValidate(model) {
+    // PATCH: Enable workers to communicate that syntax validation is complete for a specific model version
+    const modelVersionId = model.getVersionId();
     const worker = await this._worker(model.uri);
     if (model.isDisposed()) {
       return;
@@ -290,11 +292,17 @@ class DiagnosticsAdapter extends Adapter {
     if (model.isDisposed()) {
       return;
     }
+    // PATCH: Don't apply model markers if the language has changed. (Aligns this behavior with other language services.) 
+    if (model.getLanguageId() !== this._selector) {
+      return;
+    }
     editor.setModelMarkers(
       model,
       this._selector,
       diagnostics.map((d) => this._convertDiagnostics(model, d))
     );
+    // PATCH: Enable workers to communicate that syntax validation is complete for a specific model version
+    editor.setModelVersionValidated(model, modelVersionId);
   }
   _convertDiagnostics(model, diag) {
     const diagStart = diag.start || 0;
@@ -317,6 +325,8 @@ class DiagnosticsAdapter extends Adapter {
       endLineNumber,
       endColumn,
       message: flattenDiagnosticMessageText(diag.messageText, "\n"),
+      // PATCH: Add missing source property to TypeScript diagnostics to match those in VS Code derived IDEs.
+      source: 'ts',
       code: diag.code.toString(),
       tags,
       relatedInformation: this._convertRelatedInformation(model, diag.relatedInformation)
diff --git a/esm/vs/language/typescript/tsWorker.js b/esm/vs/language/typescript/tsWorker.js
index d875e075fea0eb8da7159f2168660231ad017663..7abc711c253a6a53f5a8eaf8712dd01b48ee709a 100644
--- a/esm/vs/language/typescript/tsWorker.js
+++ b/esm/vs/language/typescript/tsWorker.js
@@ -14,6 +14,13 @@ function fileNameIsLib(resource) {
   }
   return false;
 }
+// PATCH: Treat diff:// as isolated originals; excluded from diagnostics (akin to libs)
+function fileNameIsDiff(resource) {
+  if (typeof resource === 'string') {
+    return /^diff:\//.test(resource);
+  }
+  return resource.scheme === 'diff';
+}
 class TypeScriptWorker {
   constructor(ctx, createData) {
     this._extraLibs = /* @__PURE__ */ Object.create(null);
@@ -35,7 +42,8 @@ class TypeScriptWorker {
   }
   getScriptFileNames() {
     const allModels = this._ctx.getMirrorModels().map((model) => model.uri);
-    const models = allModels.filter((uri) => !fileNameIsLib(uri)).map((uri) => uri.toString());
+    // PATCH: Treat diff:// as isolated originals; excluded from diagnostics (akin to libs)
+    const models = allModels.filter((uri) => !fileNameIsDiff(uri) && !fileNameIsLib(uri)).map((uri) => uri.toString());
     return models.concat(Object.keys(this._extraLibs));
   }
   _getModel(fileName) {
@@ -162,35 +170,40 @@ class TypeScriptWorker {
     return diagnostics;
   }
   async getSyntacticDiagnostics(fileName) {
-    if (fileNameIsLib(fileName)) {
+    // PATCH: Treat diff:// as isolated originals; excluded from diagnostics (akin to libs)
+    if (fileNameIsDiff(fileName) || fileNameIsLib(fileName)) {
       return [];
     }
     const diagnostics = this._languageService.getSyntacticDiagnostics(fileName);
     return TypeScriptWorker.clearFiles(diagnostics);
   }
   async getSemanticDiagnostics(fileName) {
-    if (fileNameIsLib(fileName)) {
+    // PATCH: Treat diff:// as isolated originals; excluded from diagnostics (akin to libs)
+    if (fileNameIsDiff(fileName) || fileNameIsLib(fileName)) {
       return [];
     }
     const diagnostics = this._languageService.getSemanticDiagnostics(fileName);
     return TypeScriptWorker.clearFiles(diagnostics);
   }
   async getSuggestionDiagnostics(fileName) {
-    if (fileNameIsLib(fileName)) {
+    // PATCH: Treat diff:// as isolated originals; excluded from diagnostics (akin to libs)
+    if (fileNameIsDiff(fileName) || fileNameIsLib(fileName)) {
       return [];
     }
     const diagnostics = this._languageService.getSuggestionDiagnostics(fileName);
     return TypeScriptWorker.clearFiles(diagnostics);
   }
   async getCompilerOptionsDiagnostics(fileName) {
-    if (fileNameIsLib(fileName)) {
+    // PATCH: Treat diff:// as isolated originals; excluded from diagnostics (akin to libs)
+    if (fileNameIsDiff(fileName) || fileNameIsLib(fileName)) {
       return [];
     }
     const diagnostics = this._languageService.getCompilerOptionsDiagnostics();
     return TypeScriptWorker.clearFiles(diagnostics);
   }
   async getCompletionsAtPosition(fileName, position) {
-    if (fileNameIsLib(fileName)) {
+    // PATCH: Treat diff:// as isolated originals; excluded from diagnostics (akin to libs)
+    if (fileNameIsDiff(fileName) || fileNameIsLib(fileName)) {
       return void 0;
     }
     return this._languageService.getCompletionsAtPosition(fileName, position, void 0);
@@ -207,61 +220,71 @@ class TypeScriptWorker {
     );
   }
   async getSignatureHelpItems(fileName, position, options) {
-    if (fileNameIsLib(fileName)) {
+    // PATCH: Treat diff:// as isolated originals; excluded from diagnostics (akin to libs)
+    if (fileNameIsDiff(fileName) || fileNameIsLib(fileName)) {
       return void 0;
     }
     return this._languageService.getSignatureHelpItems(fileName, position, options);
   }
   async getQuickInfoAtPosition(fileName, position) {
-    if (fileNameIsLib(fileName)) {
+    // PATCH: Treat diff:// as isolated originals; excluded from diagnostics (akin to libs)
+    if (fileNameIsDiff(fileName) || fileNameIsLib(fileName)) {
       return void 0;
     }
     return this._languageService.getQuickInfoAtPosition(fileName, position);
   }
   async getDocumentHighlights(fileName, position, filesToSearch) {
-    if (fileNameIsLib(fileName)) {
+    // PATCH: Treat diff:// as isolated originals; excluded from diagnostics (akin to libs)
+    if (fileNameIsDiff(fileName) || fileNameIsLib(fileName)) {
       return void 0;
     }
     return this._languageService.getDocumentHighlights(fileName, position, filesToSearch);
   }
   async getDefinitionAtPosition(fileName, position) {
-    if (fileNameIsLib(fileName)) {
+    // PATCH: Treat diff:// as isolated originals; excluded from diagnostics (akin to libs)
+    if (fileNameIsDiff(fileName) || fileNameIsLib(fileName)) {
       return void 0;
     }
     return this._languageService.getDefinitionAtPosition(fileName, position);
   }
   async getReferencesAtPosition(fileName, position) {
-    if (fileNameIsLib(fileName)) {
+    // PATCH: Treat diff:// as isolated originals; excluded from diagnostics (akin to libs)
+    if (fileNameIsDiff(fileName) || fileNameIsLib(fileName)) {
       return void 0;
     }
     return this._languageService.getReferencesAtPosition(fileName, position);
   }
   async getNavigationTree(fileName) {
-    if (fileNameIsLib(fileName)) {
+    // PATCH: Treat diff:// as isolated originals; excluded from diagnostics (akin to libs)
+    if (fileNameIsDiff(fileName) || fileNameIsLib(fileName)) {
       return void 0;
     }
     return this._languageService.getNavigationTree(fileName);
   }
   async getFormattingEditsForDocument(fileName, options) {
-    if (fileNameIsLib(fileName)) {
+    // PATCH: Treat diff:// as isolated originals; excluded from diagnostics (akin to libs)
+    if (fileNameIsDiff(fileName) || fileNameIsLib(fileName)) {
       return [];
     }
     return this._languageService.getFormattingEditsForDocument(fileName, options);
   }
   async getFormattingEditsForRange(fileName, start, end, options) {
-    if (fileNameIsLib(fileName)) {
+    // PATCH: Treat diff:// as isolated originals; excluded from diagnostics (akin to libs)
+    if (fileNameIsDiff(fileName) || fileNameIsLib(fileName)) {
       return [];
     }
     return this._languageService.getFormattingEditsForRange(fileName, start, end, options);
   }
   async getFormattingEditsAfterKeystroke(fileName, postion, ch, options) {
-    if (fileNameIsLib(fileName)) {
+    // PATCH: Treat diff:// as isolated originals; excluded from diagnostics (akin to libs)
+    if (fileNameIsDiff(fileName) || fileNameIsLib(fileName)) {
       return [];
     }
     return this._languageService.getFormattingEditsAfterKeystroke(fileName, postion, ch, options);
   }
   async findRenameLocations(fileName, position, findInStrings, findInComments, providePrefixAndSuffixTextForRename) {
-    if (fileNameIsLib(fileName)) {
+    // PATCH: Treat diff:// as isolated originals; excluded from diagnostics (akin to libs)
+    if (fileNameIsDiff(fileName) || fileNameIsLib(fileName)) {
       return void 0;
     }
     return this._languageService.findRenameLocations(
@@ -273,13 +296,15 @@ class TypeScriptWorker {
     );
   }
   async getRenameInfo(fileName, position, options) {
-    if (fileNameIsLib(fileName)) {
+    // PATCH: Treat diff:// as isolated originals; excluded from diagnostics (akin to libs)
+    if (fileNameIsDiff(fileName) || fileNameIsLib(fileName)) {
       return { canRename: false, localizedErrorMessage: "Cannot rename in lib file" };
     }
     return this._languageService.getRenameInfo(fileName, position, options);
   }
   async getEmitOutput(fileName, emitOnlyDtsFiles, forceDtsEmit) {
-    if (fileNameIsLib(fileName)) {
+    // PATCH: Treat diff:// as isolated originals; excluded from diagnostics (akin to libs)
+    if (fileNameIsDiff(fileName) || fileNameIsLib(fileName)) {
       return { outputFiles: [], emitSkipped: true };
     }
     const emitOutput = this._languageService.getEmitOutput(
@@ -291,7 +316,8 @@ class TypeScriptWorker {
     return { ...emitOutput, diagnostics };
   }
   async getCodeFixesAtPosition(fileName, start, end, errorCodes, formatOptions) {
-    if (fileNameIsLib(fileName)) {
+    // PATCH: Treat diff:// as isolated originals; excluded from diagnostics (akin to libs)
+    if (fileNameIsDiff(fileName) || fileNameIsLib(fileName)) {
       return [];
     }
     const preferences = {};
@@ -312,7 +338,8 @@ class TypeScriptWorker {
     this._extraLibs = extraLibs;
   }
   async provideInlayHints(fileName, start, end) {
-    if (fileNameIsLib(fileName)) {
+    // PATCH: Treat diff:// as isolated originals; excluded from diagnostics (akin to libs)
+    if (fileNameIsDiff(fileName) || fileNameIsLib(fileName)) {
       return [];
     }
     const preferences = this._inlayHintsOptions ?? {};
diff --git a/esm/vs/platform/actionWidget/browser/actionList.js b/esm/vs/platform/actionWidget/browser/actionList.js
index 414405319ded9b3671a070b7405b7150afaa8321..c8b924d17e513acb02b8f8a74b75d8a7f22433d7 100644
--- a/esm/vs/platform/actionWidget/browser/actionList.js
+++ b/esm/vs/platform/actionWidget/browser/actionList.js
@@ -253,8 +253,9 @@ let ActionList = class ActionList extends Disposable {
         else {
             // For finding width dynamically (not using resize observer)
             const itemWidths = this._allMenuItems.map((_, index) => {
+                // PATCH: support lookup / measurement when hosted in a shadow DOM
                 // eslint-disable-next-line no-restricted-syntax
-                const element = this.domNode.ownerDocument.getElementById(this._list.getElementID(index));
+                const element = this.domNode.getRootNode().querySelector(`#${this._list.getElementID(index)}`);
                 if (element) {
                     element.style.width = 'auto';
                     const width = element.getBoundingClientRect().width;
