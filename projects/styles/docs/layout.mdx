import { Canvas, Meta, Story } from '@storybook/addon-docs';
import '@nvidia-elements/core/button/define.js';
import { GapXxxs, GapXxs, GapXs, GapSm, GapMd, GapLg, GapXl, GapXxl, GapXxxl, PadTop, PadLeft, PadRight, PadBottom, PadX, PadY, PadXxxs, PadXxs, PadXs, PadSm, PadMd, PadLg, PadXl, PadXxl, PadXxxl, Full } from './layout.stories';

<Meta title="Foundations/Layout/Documentation" />

# Layout System

Grid, horizontal and vertical based layouts can be achieved with _little to no CSS_ using the `nve-layout` attribute.
Layouts support `column`, `row` and `grid` based layouts as well as `align` and `gap` adjustments.

```html
<div nve-layout="row gap:sm">
  <nve-button></nve-button>
  <nve-button></nve-button>
  <nve-button></nve-button>
</div>
```

## Installation

```shell
npm install @nvidia-elements/styles @nvidia-elements/themes
```

```css
/* import the global CSS into your project (import may vary based on build tools) */
@import '@nvidia-elements/styles/dist/layout.css';
```

## Layout Gap Spacing

Add the `gap` value to `mlv-layout` to set spacing between elements. For example:

```html
<section mlv-layout="row gap:md">

<section mlv-layout="grid gap:md">
```

The following "t-shirt" spacing values are available:

- `gap:xxxs`
- `gap:xxs`
- `gap:xs`
- `gap:sm`
- `gap:md`
- `gap:lg`
- `gap:xl`
- `gap:xxl`
- `gap:xxxl`

<Canvas of={GapXxxs} />

<Canvas of={GapXxs} />

<Canvas of={GapXs} />

<Canvas of={GapSm} />

<Canvas of={GapMd} />

<Canvas of={GapLg} />

<Canvas of={GapXl} />

<Canvas of={GapXxl} />

<Canvas of={GapXxxl} />

## Layout Padding

Add the `pad` value to `mlv-layout` to set spacing between elements. For example:

```html
<section mlv-layout="row pad:md">

<section mlv-layout="grid pad:md">
```

The following "t-shirt" spacing values are available:

- `pad:xxxs`
- `pad:xxs`
- `pad:xs`
- `pad:sm`
- `pad:md`
- `pad:lg`
- `pad:xl`
- `pad:xxl`
- `pad:xxxl`

By default padding will apply to all 4 sides of container, to specify padding on a single side use:

- `pad-top:md`
- `pad-right:md`
- `pad-bottom:md`
- `pad-left:md`

Or alternatively use the short hand to just pad the x and y axes.

- `pad-x:md`
- `pad-y:md`

```html
<section mlv-layout="row pad-left:md">
```

<Canvas of={PadTop} />

<Canvas of={PadLeft} />

<Canvas of={PadRight} />

<Canvas of={PadBottom} />

<Canvas of={PadX} />

<Canvas of={PadY} />

<Canvas of={PadXxxs} />

<Canvas of={PadXxs} />

<Canvas of={PadXs} />

<Canvas of={PadSm} />

<Canvas of={PadMd} />

<Canvas of={PadLg} />

<Canvas of={PadXl} />

<Canvas of={PadXxl} />

<Canvas of={PadXxxl} />

## Full width/height on container

For convenience a `mlv-layout="full"` option can be set to give the container 100% width & height.
This is often useful for giving the root element of a page full height.

```html
<section mlv-layout="full">
```

<Canvas of={Full} />
