import { Meta, Props, Story, Canvas } from '@storybook/addon-docs';
import { LinkedHeader, Status } from '../../docs/utils.mdx';

<Meta title="Patterns/Page Patterns/Documentation" parameters={{ badges: ['beta'] }} />

# Page Patterns

<p mlv-text="body" class="full-width">
  Page patterns demonstrate the composition of `MagLev Elements` with proper utilization of the attribute based
  [Layout System](./?path=/story/foundations-layout-documentation--page).

  Additionally best practices in semantic and minmial HTML structuring of a pages design is shown. <br />
  All of the example are built with around **~100 lines of HTML and nearly zero CSS**.
</p>

<LinkedHeader>Stacked Cards with Collapsable Panel</LinkedHeader>

A page pattern with header, stacked cards, and left side panel.

<Canvas>
  <Story id="patterns-page-patterns-examples--stacked-cards-with-collapsable-panel" />
</Canvas>


<LinkedHeader>Stacked Cards with Closable Panel</LinkedHeader>

A page pattern with header, stacked cards, and closable right side panel.

<Canvas>
  <Story id="patterns-page-patterns-examples--stacked-cards-with-closable-panel" />
</Canvas>


<LinkedHeader>Stacked Full-Width Cards</LinkedHeader>

A page pattern with header, and full-width stacked cards.

<Canvas>
  <Story id="patterns-page-patterns-examples--stacked-cards-full-width" />
</Canvas>

<LinkedHeader>Stacked Cards Grid</LinkedHeader>

A page pattern with header, and a mosaic grid of cards.

<Canvas>
  <Story id="patterns-page-patterns-examples--stacked-card-grid" />
</Canvas>
