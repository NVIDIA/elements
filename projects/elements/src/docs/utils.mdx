export const LinkedHeader = ({children: txt} ) => {
  return (
    <h2 id={txt.replace(/\s+/g, '-').toLowerCase()} >{txt}</h2>
  );
}

export const Status = ({children: txt, status: { demo, apiDoc, unitTests, themeSupport, apiReview, vqaReview, a11y, performance, stable, pkg, figma }} ) => {
  const alpha = apiReview && vqaReview && a11y && performance && stable && pkg && figma;
  const beta = apiReview && vqaReview && a11y && performance;
  const stbl = stable && pkg && figma;
  return (
    <div class="status-summary" style={{ margin: '0 0 80px 0' }}>
      <h2>Stability</h2>
      <p>All elements and features go through 3 phases of stability, pre-release, beta and stable.</p>
      <h3><nve-badge status={(!beta && !stbl) || demo ? 'warning' : 'pending'}>pre-release <nve-icon name="warning"></nve-icon></nve-badge></h3>
      <div style={{ display: 'flex', flexDirection: 'column', gap: '8px' }}>
        <nve-alert status={unitTests ? 'finished' : 'pending'}>Robust unit test coverage</nve-alert>
        <nve-alert status={themeSupport ? 'finished' : 'pending'}>Theme support</nve-alert>
        <nve-alert status={themeSupport ? 'finished' : 'pending'}>Adapt to different screen/container sizes</nve-alert>
      </div>
      <h3><nve-badge status={(beta && !stbl) || demo ? 'running' : 'pending'}>beta <nve-icon name="schedule"></nve-icon></nve-badge></h3>
      <div style={{ display: 'flex', flexDirection: 'column', gap: '8px' }}>
        <nve-alert status={a11y ? 'finished' : 'pending'}>No outstanding <a href="https://www.w3.org/WAI/ARIA/apg/">AA WCAG issues</a></nve-alert>
        <nve-alert status={performance ? 'finished' : 'pending'}>No outstanding performance issues</nve-alert>
        <nve-alert status={apiReview ? 'finished' : 'pending'}>Passed <a href="./?path=/story/about-api-design-getting-started--page">Public API Review</a></nve-alert>
        <nve-alert status={vqaReview ? 'finished' : 'pending'}>Passed VQA Review</nve-alert>
      </div>
      <h3><nve-badge status={stbl || demo ? 'finished' : 'pending'}>stable <nve-icon name="success-badge"></nve-icon></nve-badge></h3>
      <div style={{ display: 'flex', flexDirection: 'column', gap: '8px' }}>
        <nve-alert status={pkg ? 'finished' : 'pending'}>Published in <a href="http://nv/elements">library package</a></nve-alert>
        <nve-alert status={figma ? 'finished' : 'pending'}>Published in <a href="http://nv/elements-figma">Figma</a></nve-alert>
        <nve-alert status={stable ? 'finished' : 'pending'}>No breaking API changes for at least 90 days</nve-alert>
      </div>
    </div>
  );
}