import { Meta } from '@storybook/addon-docs';
import { Canvas } from '@nve-internals/storybook/blocks';
import { Select, Multiple, MultipleOverflow, CustomOptionRender, Flat, Vertical, Horizontal, Placeholder, Disabled, Height, Size, Prefix, FitContent, FitText, Performance, PlaceholderMultiple } from './select.stories';

<Meta title="Elements/Select/Documentation" />

# Select

<nve-api-summary tag="nve-select"></nve-api-summary>

## Installation

```typescript
import '@nvidia-elements/core/select/define.js';
```

```html
<nve-select>
  <label>label</label>
  <select>
    <option value="1">Option 1</option>
    <option value="2">Option 2</option>
    <option value="3">Option 3</option>
  </select>
  <nve-control-message>message</nve-control-message>
</nve-select>
```

## Standard

<Canvas of={Select} />

## Multiple

The multiple option behavior preserves the native select value behavior. The `value` on the select
will only reflect the first selected value. To get all selected options check the `selected` property
on each `<option>` element or the select property `selectedOptions`.

<Canvas of={Multiple} />

If the multiple tags overflow the parent container a simple text label will be shown instead.

<Canvas of={MultipleOverflow} />

## Custom Option Render

Select can render custom templates for option menus with the following limitations:

- The select is for use of form control values only. For navigation use `nve-menu`.
- The select value when not active is not customizable.
- Cannot be used with `size` options.

<Canvas of={CustomOptionRender} />

## Flat

<Canvas of={Flat} />

## Layout

<nve-api-detail tag="nve-select" type="property" value="layout"></nve-api-detail>

### Vertical

<Canvas of={Vertical} />

### Horizontal

<Canvas of={Horizontal} />

## Placeholder

<Canvas of={Placeholder} />

<Canvas of={PlaceholderMultiple} />

## Disabled

<Canvas of={Disabled} />

## Height

Determines the scroll height of selection in open dropdown.

<Canvas of={Height} />

## Size

Enables [size](https://developer.mozilla.org/en-US/docs/Web/HTML/Attributes/size) attribute on the select element.
This creates a inline selection at the defined item height.

<Canvas of={Size} />

## Prefix

<Canvas of={Prefix} />

## Fit Content

<nve-api-detail tag="nve-select" type="property" value="fitContent"></nve-api-detail>

<Canvas of={FitContent} />

## Fit Text

<nve-api-detail tag="nve-select" type="property" value="fitText"></nve-api-detail>

<Canvas of={FitText} />

## Performance

<Canvas of={Performance} />

<nve-api-status tag="nve-select"></nve-api-status>

<nve-api-table tag="nve-select"></nve-api-table>
