import { Meta, ArgsTable, Story, Canvas } from '@storybook/addon-docs';
import { LinkedHeader } from '../../docs/utils.mdx';

<Meta title="Elements/Select/Documentation" parameters={{ badges: ['stable'] }} />

# Select

<element-metrics tag="nve-select"></element-metrics>

<LinkedHeader>Installation</LinkedHeader>

```typescript
import '@elements/elements/select/define.js';
```

```html
<nve-select>
  <label>label</label>
  <select>
    <option value="1">Option 1</option>
    <option value="2">Option 2</option>
    <option value="3">Option 3</option>
  </select>
  <nve-control-message>message</nve-control-message>
</nve-select>
```

<Canvas>
  <Story id="elements-select-examples--select" />
</Canvas>

<LinkedHeader>Multiple</LinkedHeader>

The multiple option behavior preserves the native select value behavior. The `value` on the select
will only reflect the first selected value. To get all selected options check the `selected` property
on each `<option>` element.

<Canvas>
  <Story id="elements-select-examples--multiple" />
</Canvas>

If the multiple chips overflow the parent container a simple text label will be shown instead.

<Canvas>
  <Story id="elements-select-examples--multiple-overflow" />
</Canvas>

<LinkedHeader>Custom Option Render</LinkedHeader>

Select can render custom templates for option menus with the following limitations:
- The select is for use of form control values only. For navigation use `nve-menu`.
- The select value when not active is not customizable.
- Cannot be used with `size` options.

<Canvas>
  <Story id="elements-select-examples--custom-option-render" />
</Canvas>

<LinkedHeader>Vertical</LinkedHeader>

<Canvas>
  <Story id="elements-select-examples--vertical" />
</Canvas>

<LinkedHeader>Horizontal</LinkedHeader>

<Canvas>
  <Story id="elements-select-examples--horizontal" />
</Canvas>

<LinkedHeader>Size</LinkedHeader>

<Canvas>
  <Story id="elements-select-examples--size" />
</Canvas>

<LinkedHeader>Height</LinkedHeader>

<Canvas>
  <Story id="elements-select-examples--height" />
</Canvas>

<LinkedHeader>Prefix</LinkedHeader>

<Canvas>
  <Story id="elements-select-examples--prefix" />
</Canvas>

<element-status tag={'nve-select'}></element-status>

<LinkedHeader>nve-select</LinkedHeader>

<ArgsTable of="nve-select" />
