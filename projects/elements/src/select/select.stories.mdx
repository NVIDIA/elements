import { Meta, ArgsTable, Story, Canvas } from '@storybook/addon-docs';
import { statusBadges } from '../../docs/utils.mdx';

<Meta title="Elements/Select/Documentation" parameters={{ badges: statusBadges('mlv-select') }} />

# Select

<element-metrics tag="mlv-select"></element-metrics>

## Installation

```typescript
import '@elements/elements/select/define.js';
```

```html
<mlv-select>
  <label>label</label>
  <select>
    <option value="1">Option 1</option>
    <option value="2">Option 2</option>
    <option value="3">Option 3</option>
  </select>
  <mlv-control-message>message</mlv-control-message>
</mlv-select>
```

## Standard

<Canvas>
  <Story id="elements-select-examples--select" />
</Canvas>

## Multiple

The multiple option behavior preserves the native select value behavior. The `value` on the select
will only reflect the first selected value. To get all selected options check the `selected` property
on each `<option>` element or the select property `selectedOptions`.

<Canvas>
  <Story id="elements-select-examples--multiple" />
</Canvas>

If the multiple tags overflow the parent container a simple text label will be shown instead.

<Canvas>
  <Story id="elements-select-examples--multiple-overflow" />
</Canvas>

## Custom Option Render

Select can render custom templates for option menus with the following limitations:
- The select is for use of form control values only. For navigation use `mlv-menu`.
- The select value when not active is not customizable.
- Cannot be used with `size` options.

<Canvas>
  <Story id="elements-select-examples--custom-option-render" />
</Canvas>

## Flat

<Canvas>
  <Story id="elements-select-examples--flat" />
</Canvas>

## Layout

<element-api tag="mlv-select" type="property" value="layout"></element-api>

### Vertical

<Canvas>
  <Story id="elements-select-examples--vertical" />
</Canvas>

### Horizontal

<Canvas>
  <Story id="elements-select-examples--horizontal" />
</Canvas>

## Placeholder

<Canvas>
  <Story id="elements-select-examples--placeholder" />
</Canvas>

## Disabled

<Canvas>
  <Story id="elements-select-examples--disabled" />
</Canvas>

## Height

<Canvas>
  <Story id="elements-select-examples--height" />
</Canvas>

## Prefix

<Canvas>
  <Story id="elements-select-examples--prefix" />
</Canvas>

## Fit Content

<element-api tag="mlv-select" type="property" value="fitContent"></element-api>

<Canvas>
  <Story id="elements-select-examples--fit-content" />
</Canvas>

## Fit Text

<element-api tag="mlv-select" type="property" value="fitText"></element-api>

<Canvas>
  <Story id="elements-select-examples--fit-text" />
</Canvas>

## Performance

<Canvas>
  <Story id="elements-select-examples--performance" />
</Canvas>

<element-status tag="mlv-select"></element-status>

<element-api tag="mlv-select"></element-api>
