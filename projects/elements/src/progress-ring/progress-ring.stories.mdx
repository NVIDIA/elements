import { Meta, ArgsTable, Story, Canvas } from '@storybook/addon-docs';
import { statusBadges } from '../../docs/utils.mdx';

<Meta title="Elements/Progress Ring/Documentation" parameters={{ badges: statusBadges('nve-progress-ring') }} />

# Progress Ring

<element-metrics tag="nve-progress-ring"></element-metrics>

## Installation

```typescript
import '@elements/elements/progress-ring/define.js';
```

```html
<nve-progress-ring></nve-progress-ring>
```

# Setting the Value

<element-metrics tag="nve-progress-ring" type="property" value="value"></element-metrics>

<Canvas>
  <Story id="elements-progress-ring-examples--values" />
</Canvas>

<Canvas>
  <Story id="elements-progress-ring-examples--default" />
</Canvas>

# Max Value

<element-metrics tag="nve-progress-ring" type="property" value="max"></element-metrics>

<Canvas>
  <Story id="elements-progress-ring-examples--max" />
</Canvas>

# Indicating Status

<element-metrics tag="nve-progress-ring" type="property" value="status"></element-metrics>

<Canvas>
  <Story id="elements-progress-ring-examples--status" />
</Canvas>

When `value` is set to zero, full size status icons are shown with no ring.

<Canvas>
  <Story id="elements-progress-ring-examples--zero-value-status" />
</Canvas>

When used with text, please set `size="xs"`.

<Canvas>
  <Story id="elements-progress-ring-examples--with-text" />
</Canvas>

# Sizing

<element-metrics tag="nve-progress-ring" type="property" value="size"></element-metrics>

<Canvas>
  <Story id="elements-progress-ring-examples--sizing" />
</Canvas>

# Custom Icon Slotting

The `status-icon` slot can be used within `nve-progress-ring` to create a custom loading indicator.

<Canvas>
  <Story id="elements-progress-ring-examples--slotted-icon" />
</Canvas>

# Using within a Button

When `nve-progress-ring` is used within a `nve-button` the `status` should be set to `neutral` to properly inherit button background color.

<Canvas>
  <Story id="elements-progress-ring-examples--with-button" />
</Canvas>

<element-status tag={'nve-progress-ring'}></element-status>

## nve-progress-ring

<ArgsTable of="nve-progress-ring" />