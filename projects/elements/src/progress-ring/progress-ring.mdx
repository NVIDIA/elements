import { Meta } from '@storybook/addon-docs';
import { Canvas } from '@internals/storybook/blocks';
import { Default, Values, Max, Status, ZeroValueStatus, WithText, Sizing, SlottedIcon, WithButton } from './progress-ring.stories';

<Meta title="Elements/Progress Ring/Documentation" />

# Progress Ring

<element-metrics tag="nve-progress-ring"></element-metrics>

## Installation

```typescript
import '@nvidia-elements/core/progress-ring/define.js';
```

```html
<nve-progress-ring></nve-progress-ring>
```

## Standard

<Canvas of={Default} />

## Value

<element-api tag="nve-progress-ring" type="property" value="value"></element-api>

<Canvas of={Values} />

## Max Value

<element-api tag="nve-progress-ring" type="property" value="max"></element-api>

<Canvas of={Max} />

## Indicating Status

<element-api tag="nve-progress-ring" type="property" value="status"></element-api>

<Canvas of={Status} />

When `value` is set to zero, full size status icons are shown with no ring.

<Canvas of={ZeroValueStatus} />

When used with text, please set `size="xs"`.

<Canvas of={WithText} />

## Sizing

<element-api tag="nve-progress-ring" type="property" value="size"></element-api>

<Canvas of={Sizing} />

## Custom Icon Slotting

The `status-icon` slot can be used within `nve-progress-ring` to create a custom loading indicator.

<Canvas of={SlottedIcon} />

## Using within a Button

When `nve-progress-ring` is used within a `nve-button` the `status` should be set to `neutral` to properly inherit button background color.

<Canvas of={WithButton} />

<element-status tag="nve-progress-ring"></element-status>

<element-api tag="nve-progress-ring"></element-api>
