import { Meta, Canvas } from '@storybook/addon-docs';
import { Default, Values, Max, Status, ZeroValueStatus, WithText, Sizing, SlottedIcon, WithButton } from './progress-ring.stories';

<Meta title="Elements/Progress Ring/Documentation" />

# Progress Ring

<element-metrics tag="mlv-progress-ring"></element-metrics>

## Installation

```typescript
import '@elements/elements/progress-ring/define.js';
```

```html
<mlv-progress-ring></mlv-progress-ring>
```

## Standard

<Canvas of={Default} />

## Value

<element-api tag="mlv-progress-ring" type="property" value="value"></element-api>

<Canvas of={Values} />

## Max Value

<element-api tag="mlv-progress-ring" type="property" value="max"></element-api>

<Canvas of={Max} />

## Indicating Status

<element-api tag="mlv-progress-ring" type="property" value="status"></element-api>

<Canvas of={Status} />

When `value` is set to zero, full size status icons are shown with no ring.

<Canvas of={ZeroValueStatus} />

When used with text, please set `size="xs"`.

<Canvas of={WithText} />

## Sizing

<element-api tag="mlv-progress-ring" type="property" value="size"></element-api>

<Canvas of={Sizing} />

## Custom Icon Slotting

The `status-icon` slot can be used within `mlv-progress-ring` to create a custom loading indicator.

<Canvas of={SlottedIcon} />

## Using within a Button

When `mlv-progress-ring` is used within a `mlv-button` the `status` should be set to `neutral` to properly inherit button background color.

<Canvas of={WithButton} />

<element-status tag="mlv-progress-ring"></element-status>

<element-api tag="mlv-progress-ring"></element-api>
