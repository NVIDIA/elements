import { Meta, Story } from '@storybook/addon-docs';
import { Canvas } from '@nve-internals/storybook/blocks';
import { Default, MediaCard, CardWithHeader, CardWithHeaderAndAction, Tabs, DescriptionList, CardWithHeaderAndFooter, CardWithMultipleContentsAndDivider, CardWithContentLayout, ContainerFill } from './card.stories';

<Meta title="Elements/Card/Documentation" />

# Card

<nve-api-summary tag="nve-card"></nve-api-summary>

## Installation

```typescript
import '@nvidia-elements/core/card/define.js';
```

```html
<nve-card>
  <nve-card-content>
    Card Content
  </nve-card-content>
</nve-card>
```

## Standard

<Canvas of={Default} />

## Media Card

<Canvas of={MediaCard} />

## Card with Header

<Canvas of={CardWithHeader} />

## Card with Header and Action

<Canvas of={CardWithHeaderAndAction} />

## Card with Header Tabs

<Canvas of={Tabs} />

## Card Description List

<Canvas of={DescriptionList} />

## Card with Header and Footer

Combining `card-header`, `card-content` & `card-footer` will yield a fully styled card.
Note how `card-content` will vertically stretch to fill the card size and the footer will align
to the bottom of the card.

<Canvas of={CardWithHeaderAndFooter} />

## Multiple Content Containers and Full-bleed Divider

<Canvas of={CardWithMultipleContentsAndDivider} />

## Content Layout

Layout system is supported when `nve-layout` attribute is used on the `card-content` element:

<Canvas of={CardWithContentLayout} />

## Container Fill

<Canvas of={ContainerFill} />

<nve-api-status tag="nve-card"></nve-api-status>

<nve-api-table tag="nve-card"></nve-api-table>

<nve-api-table tag="nve-card-header"></nve-api-table>

<nve-api-table tag="nve-card-content"></nve-api-table>

<nve-api-table tag="nve-card-footer"></nve-api-table>
