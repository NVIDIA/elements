import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { Animation } from '../../../docs/tokens.stories';
import { ReducedMotion } from '../../../docs/theme.stories';

<Meta title="Foundations/Tokens/Animation" />

# Animation

Animation tokens provide basic timings for animations and hook into theme support for reduced motion.

<Story of={Animation} />

## Reduced Motion

<Canvas of={ReducedMotion} />
