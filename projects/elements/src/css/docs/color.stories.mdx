import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { LinkedHeader } from '../../../docs/utils.mdx';

<Meta title="Foundations/Tokens/Color" parameters={{ badges: ['beta'] }} />

# Color Palette

<p nve-text="body" class="full-width">
  Colors should be used via the system tokens (--nve-sys-*) when possible to ensure the appropriate value is used in a semantic and meaningful way.
</p>

<Story id="foundations-tokens-examples--color-palette" inline={false} height={'4000px'} />

<Story id="internal-integration--empty" />
