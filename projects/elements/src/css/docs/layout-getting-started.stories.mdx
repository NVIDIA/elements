import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { LinkedHeader } from '../../docs/utils.mdx';

<Meta title="Foundations/Layout/Documentation" />

# Layout System

Grid, horizontal and vertical based layouts can be achieved with _little to no CSS_ using the `mlv-layout` attribute.

Set `mlv-layout="row"` `mlv-layout="column"` or `mlv-layout="grid"` on a container element and add additional alignment values.

- `mlv-layout="row ..."`
- `mlv-layout="column ..."`
- `mlv-layout="grid ..."`


- `align:left`
- `align:right`
- `align:top`
- `align:bottom ...`

Combine `mlv-layout` attribute values to create your desired layout. For example:
```html
<section mlv-layout="row align:vertical-center align:right">
```

<LinkedHeader>Framework Integrations</LinkedHeader>

Some frameworks and libraries may require additional configuration to use the `mlv-layout` attribute. See the links below for specific integration patterns for the following frameworks:

<mlv-button>
  <a href="?path=/story/integrations-lit--page#css-utilities"><svg width="20" height="20"><use xmlnsXlink="http://www.w3.org/1999/xlink" xlinkHref="#lit-svg"></use></svg> Lit Integration</a>
</mlv-button>

<LinkedHeader>Layout Gap Spacing</LinkedHeader>

Add the `gap` value to `mlv-layout` to set spacing between elements. For example:
```html
<section mlv-layout="row gap:md">
```
```html
<section mlv-layout="grid gap:md">
```

The following "t-shirt" spacing values are available:

- `gap:xxxs`
- `gap:xxs`
- `gap:xs`
- `gap:sm`
- `gap:md`
- `gap:lg`
- `gap:xl`
- `gap:xxl`
- `gap:xxxl`

<Canvas>
  <Story id="internal-layout-examples--gap-xxxs" />
</Canvas>

<Canvas>
  <Story id="internal-layout-examples--gap-xxs" />
</Canvas>

<Canvas>
  <Story id="internal-layout-examples--gap-xs" />
</Canvas>

<Canvas>
  <Story id="internal-layout-examples--gap-sm" />
</Canvas>

<Canvas>
  <Story id="internal-layout-examples--gap-md" />
</Canvas>

<Canvas>
  <Story id="internal-layout-examples--gap-lg" />
</Canvas>

<Canvas>
  <Story id="internal-layout-examples--gap-xl" />
</Canvas>

<Canvas>
  <Story id="internal-layout-examples--gap-xxl" />
</Canvas>

<Canvas>
  <Story id="internal-layout-examples--gap-xxxl" />
</Canvas>


<LinkedHeader>Layout Padding</LinkedHeader>


Add the `pad` value to `mlv-layout` to set spacing between elements. For example:
```html
<section mlv-layout="row pad:md">
```
```html
<section mlv-layout="grid pad:md">
```

The following "t-shirt" spacing values are available:

- `pad:xxxs`
- `pad:xxs`
- `pad:xs`
- `pad:sm`
- `pad:md`
- `pad:lg`
- `pad:xl`
- `pad:xxl`
- `pad:xxxl`

By default padding will apply to all 4 sides of container, to specify padding on a single side use:

- `pad-top:md`
- `pad-right:md`
- `pad-bottom:md`
- `pad-left:md`

```html
<section mlv-layout="row pad-left:md">
```



<Canvas>
  <Story id="internal-layout-examples--pad-xxxs" />
</Canvas>

<Canvas>
  <Story id="internal-layout-examples--pad-xxs" />
</Canvas>

<Canvas>
  <Story id="internal-layout-examples--pad-xs" />
</Canvas>

<Canvas>
  <Story id="internal-layout-examples--pad-sm" />
</Canvas>

<Canvas>
  <Story id="internal-layout-examples--pad-md" />
</Canvas>

<Canvas>
  <Story id="internal-layout-examples--pad-lg" />
</Canvas>

<Canvas>
  <Story id="internal-layout-examples--pad-xl" />
</Canvas>

<Canvas>
  <Story id="internal-layout-examples--pad-xxl" />
</Canvas>

<Canvas>
  <Story id="internal-layout-examples--pad-xxxl" />
</Canvas>


<LinkedHeader>Full width/height on container</LinkedHeader>

For convenience a `mlv-layout="grow"` option can be set to give the container `100% width & height`.
This is often useful for giving the root element of a page full height.

```html
<section mlv-layout="grow">
```

<Canvas>
  <Story id="internal-layout-examples--grow" />
</Canvas>
