import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { LinkedHeader } from '../../docs/utils.mdx';

<Meta title="Foundations/Tokens/Layers" />

# Layers (beta)

Layers define the position of an element on the z-axis in the UI. Layer types are
categories of elements that build up to higher layers as they are stacked or nested.
Read more about the [popover explainer](https://open-ui.org/components/popup.research.explainer).

The layers in order of stacking are the following:

- `canvas` - body, document
- `container` - cards, steppers, tabs
- `overlay` - modals, drawers, dropdowns (menus, filters, combobox)
- `popover` - tooltips, toasts, notifications

<Canvas>
  <Story id="foundations-tokens-examples--layers-demo" inline={false} height="1200px" />
</Canvas>

<Canvas>
  <Story id="foundations-tokens-examples--layers" />
</Canvas>

<LinkedHeader>Layer Tokens</LinkedHeader>

<Canvas>
  <Story id="foundations-tokens-examples--layer" />
</Canvas>

<LinkedHeader>Interaction Tokens</LinkedHeader>

<Canvas>
  <Story id="foundations-tokens-examples--interaction" />
</Canvas>