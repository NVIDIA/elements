import { Canvas, Meta } from '@storybook/addon-docs';
import { ColorPalette } from '../../../docs/tokens.stories';

<Meta title="Foundations/Tokens/Color" />

# Color Palette

<p mlv-text="body" className="full-width">
  Colors should be used via the system tokens (--mlv-sys-*) when possible to ensure the appropriate value is used in a semantic and meaningful way.
</p>

<Canvas of={ColorPalette} />
