import { Meta, Story } from '@storybook/addon-docs';
import { Canvas } from '@nve-internals/storybook/blocks';
import { Default, Visual, Content, Position, Events, Wrap, Hint, Status, OpenDelay, Alignment, DynamicTrigger, DynamicAnchorPosition } from './tooltip.stories';

<Meta title="Elements/Tooltip/Documentation" />

# Tooltip

<element-metrics tag="nve-tooltip"></element-metrics>

## Installation

```typescript
import '@nvidia-elements/core/tooltip/define.js';
```

```html
<nve-tooltip id="tooltip">tooltip</nve-tooltip>
<nve-button popovertarget="tooltip">button</nve-button>
```

## Standard

<Canvas of={Default}  />

<Canvas of={Visual} story={{ inline: false, height: 130 }} />

## Position

<element-api tag="nve-tooltip" type="property" value="position"></element-api>

<Canvas of={Position} story={{ inline: false, height: 280 }} />

## Alignment

<element-api tag="nve-tooltip" type="property" value="alignment"></element-api>

<Canvas of={Alignment} story={{ inline: false, height: 350 }} />

## Events

<element-api tag="nve-tooltip" type="event" value="open"></element-api>

<element-api tag="nve-tooltip" type="event" value="close"></element-api>

<Canvas of={Events} story={{ inline: false, height: 280 }} />

## Dynamic Trigger

A popover can be recycled for multiple triggers.

<Canvas of={DynamicTrigger} story={{ inline: false, height: 300 }} />

## Open Delay

Optionally, add a delay to tooltip opening, with `open-delay` attribute (set in milliseconds) -
useful when displaying tooltips on datagrid items to avoid overwhelming the UX as the user hovers over rows.

<Canvas of={OpenDelay} story={{ inline: false, height: 300 }} />

## Wrap

<Canvas of={Wrap} story={{ inline: false, height: 280 }} />

## Content

<Canvas of={Content} story={{ inline: false, height: 280 }} />

## Status

<Canvas of={Status} story={{ inline: false, height: 300 }} />

## Hint

<Canvas of={Hint} story={{ inline: false, height: 200 }} />

## Dynamic Anchor Position

A popover will follow its assigned anchor event when the anchor position is dynamically changed.

<Canvas of={DynamicAnchorPosition} story={{ inline: false, height: 600 }} />

```typescript
@customElement('dynamic-anchor-position-demo')
class DynamicAnchorPositionDemo extends LitElement {
  #anchor: Ref<HTMLElement> = createRef();

  render() {
    return html`
      <div ${ref(this.#anchor)} id="anchor"></div>
      <nve-tooltip anchor="anchor">tooltip</nve-tooltip>
    `;
  }

  firstUpdated(props: PropertyValues<this>) {
    super.firstUpdated(props);
    this.addEventListener('mousemove', e => this.#anchor.value.style.inset = `${e.clientY - 15}px auto auto ${e.clientX - 15}px`);
  }
}
```

<element-status tag="nve-tooltip"></element-status>

<element-api tag="nve-tooltip"></element-api>
