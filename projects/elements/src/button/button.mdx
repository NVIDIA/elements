import { Meta, Story } from '@storybook/addon-docs';
import { Canvas } from '@internals/storybook/blocks';
import { Default, Interaction, Flat, Inline, Pressed, SelectedFlat, Size, Link, LinkFlat, ButtonWithIcon, FormControl } from './button.stories';

<Meta title="Elements/Button/Documentation" />

# Button

<element-metrics tag="nve-button"></element-metrics>

## Installation

```typescript
import '@nvidia-elements/core/button/define.js';
```

```html
<nve-button>button</nve-button>
```

## Standard

<Canvas of={Default} />

## Interaction

<element-api tag="nve-button" type="property" value="interaction"></element-api>

### Standard

<Canvas of={Interaction} />

### Flat

<element-api tag="nve-button" type="property" value="container"></element-api>

<Canvas of={Flat} />

### Inline

<element-api tag="nve-button" type="property" value="container"></element-api>

<Canvas of={Inline} />

## Pressed

<element-api tag="nve-button" type="property" value="pressed"></element-api>

<Canvas of={Pressed} />

## Selected

<element-api tag="nve-button" type="property" value="selected"></element-api>

<Canvas of={SelectedFlat} />

## Size

<element-api tag="nve-button" type="property" value="size"></element-api>

<Canvas of={Size} />

## Linked Buttons

Anchors can wrap button instances, however its recommended to slot the anchor into the button.

```html
<nve-button><a href="#">default</a></nve-button> <!-- do -->
<a href="#"><nve-button>default</nve-button></a> <!-- don't -->
```

If you need to slot an icon in a linked button, put `nve-icon` adjacent to the anchor tag.

```html
<nve-button><a href="#">default</a> <nve-icon name="reset"></nve-icon></nve-button> <!-- do -->
```

<Canvas of={Link} />

<Canvas of={LinkFlat} />

## With Icon

<Canvas of={ButtonWithIcon} />

## Form Control

Form control option allows a button to be styled like a control field. This is helpful for using button to trigger custom form control components or dropdowns.

<Canvas of={FormControl} />

<element-status tag="nve-button"></element-status>

<element-api tag="nve-button"></element-api>
