import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { Default, Interaction, Flat, Inline, PressedToggle, SelectedFlat, Size, Link, LinkFlat, ButtonWithIcon, FormControl } from './button.stories';

<Meta title="Elements/Button/Documentation" />

# Button

<element-metrics tag="mlv-button"></element-metrics>

## Installation

```typescript
import '@nvidia-elements/core/button/define.js';
```

```html
<mlv-button>button</mlv-button>
```

## Standard

<Canvas of={Default} />

## Interaction

<element-api tag="mlv-button" type="property" value="interaction"></element-api>

### Standard

<Canvas of={Interaction} />

### Flat

<element-api tag="mlv-button" type="property" value="container"></element-api>

<Canvas of={Flat} />

### Inline

<element-api tag="mlv-button" type="property" value="container"></element-api>

<Canvas of={Inline} />

## Selection Types

### Pressed

<element-api tag="mlv-button" type="property" value="pressed"></element-api>

<Canvas of={PressedToggle} />

### Selected Flat

<element-api tag="mlv-button" type="property" value="selected"></element-api>

<Canvas of={SelectedFlat} />

## Size

<element-api tag="mlv-button" type="property" value="size"></element-api>

<Canvas of={Size} />

## Linked Buttons

Anchors can wrap button instances, however its recommended to slot the anchor into the button.

```html
<mlv-button><a href="#">default</a></mlv-button> <!-- do -->
<a href="#"><mlv-button>default</mlv-button></a> <!-- don't -->
```

If you need to slot an icon in a linked button, put `<mlv-icon>` adjacent to the anchor tag.

```html
<mlv-button><a href="#">default</a> <mlv-icon name="reset"></mlv-icon></mlv-button> <!-- do -->
```

<Canvas of={Link} />

<Canvas of={LinkFlat} />

## With Icon

<Canvas of={ButtonWithIcon} />

## Form Control

Form control option allows a button to be styled like a control field. This is helpful for using button to trigger custom form control components or dropdowns.

<Canvas of={FormControl} />

<element-status tag="mlv-button"></element-status>

<element-api tag="mlv-button"></element-api>
