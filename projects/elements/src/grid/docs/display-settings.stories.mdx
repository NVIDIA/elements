import { Meta, ArgsTable, Story, Canvas } from '@storybook/addon-docs';
import { statusBadges } from '../../../docs/utils.mdx';

<Meta title="Elements/Data Grid/Display Settings" parameters={{ badges: statusBadges('nve-grid') }} />

# Data Grid Display Settings

<element-metrics tag="nve-grid-column"></element-metrics>

Display settings are placed above the data grid leveraging dropdowns to provide various configuration options.
Columns can be hidden via the `hidden` attribute or conditionaly rendered in DOM. Display settings can include
column visibility or advanced filtering options.

```html
<section nve-layout="row gap:md align:stretch">
  <nve-panel expanded closable>
    ...
  </nve-panel>

  <div nve-layout="column gap:md">
    <nve-button>display settings</nve-button>
    <nve-grid>
      ...
    </nve-grid>
  </div>
</section>
```

<Canvas>
  <Story id="elements-data-grid-examples--display-settings" inline={false} height="450px" />
</Canvas>

## nve-grid-column
<ArgsTable of="nve-grid-column" />

## nve-grid
<ArgsTable of="nve-grid" />

## nve-grid-header
<ArgsTable of="nve-grid-header" />

## nve-grid-row
<ArgsTable of="nve-grid-row" />

## nve-grid-cell
<ArgsTable of="nve-grid-cell" />