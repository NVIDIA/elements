import { Meta, ArgsTable, Story, Canvas } from '@storybook/addon-docs';
import { statusBadges } from '../../../docs/utils.mdx';

<Meta title="Elements/Data Grid/Display Settings" parameters={{ badges: statusBadges('mlv-grid') }} />

# Data Grid Display Settings

<element-metrics tag="mlv-grid-column"></element-metrics>

Display settings are placed above the data grid leveraging dropdowns to provide various configuration options.
Columns can be hidden via the `hidden` attribute or conditionaly rendered in DOM. Display settings can include
column visibility or advanced filtering options.

```html
<section mlv-layout="row gap:md align:stretch">
  <mlv-panel expanded closable>
    ...
  </mlv-panel>

  <div mlv-layout="column gap:md">
    <mlv-button>display settings</mlv-button>
    <mlv-grid>
      ...
    </mlv-grid>
  </div>
</section>
```

<Canvas>
  <Story id="elements-data-grid-examples--display-settings" inline={false} height="450px" />
</Canvas>

## mlv-grid-column
<ArgsTable of="mlv-grid-column" />

## mlv-grid
<ArgsTable of="mlv-grid" />

## mlv-grid-header
<ArgsTable of="mlv-grid-header" />

## mlv-grid-row
<ArgsTable of="mlv-grid-row" />

## mlv-grid-cell
<ArgsTable of="mlv-grid-cell" />