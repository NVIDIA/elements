import { Meta, Canvas } from '@storybook/addon-docs';
import { Scroll, FullHeight, ScrollPosition } from './grid.stories';

<Meta title="Elements/Data Grid/Scroll Height" />

# Data Grid Scroll Height

<element-metrics tag="mlv-grid"></element-metrics>

A fixed height can be placed on the grid allowing rows to be scrolled within the bounding box of the grid.

```html
<mlv-grid style="height: 400px">
  <mlv-grid-header>
    <mlv-grid-column>column 1</mlv-grid-column>
    <mlv-grid-column>column 2</mlv-grid-column>
    <mlv-grid-column>column 3</mlv-grid-column>
    <mlv-grid-column>column 4</mlv-grid-column>
  </mlv-grid-header>
  <mlv-grid-row>
    <mlv-grid-cell>cell 1-1</mlv-grid-cell>
    <mlv-grid-cell>cell 1-2</mlv-grid-cell>
    <mlv-grid-cell>cell 1-3</mlv-grid-cell>
    <mlv-grid-cell>cell 1-4</mlv-grid-cell>
  </mlv-grid-row>
  ...
</mlv-grid>
```

<Canvas of={Scroll} />

## Full Height

Using `mlv-layout="column"` and `mlv-layout="full"` enables the grid to fill any remaining space of a parent containing element.
This is helpful for preserving the grid height/fill while dynamic content above can freely change.

<Canvas of={FullHeight} />

## Scroll Position

Scroll position can be controlled via the `scrollTo` API.

```javascript
document.querySelector('mlv-grid').scrollTo({ top: 0, left: 0, behavior: 'smooth' });
```

<Canvas of={ScrollPosition} story={{ inline: false, height: '500px' }} />

<element-status tag="mlv-grid"></element-status>

<element-api tag="mlv-grid"></element-api>

<element-api tag="mlv-grid-header"></element-api>

<element-api tag="mlv-grid-column"></element-api>

<element-api tag="mlv-grid-row"></element-api>

<element-api tag="mlv-grid-cell"></element-api>
