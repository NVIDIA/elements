import { Meta, Canvas } from '@storybook/addon-docs';
import { Card, CardTabs } from './grid.stories';

<Meta title="Elements/Data Grid/Card" />

# Data Grid Card

<element-metrics tag="mlv-grid"></element-metrics>

Grid can be nested in Cards for various UI patterns such as card tab groups. Use the `container="flat"` attribute to
enable proper styling of the grid when nested within a card.

```html
<mlv-card>
  <mlv-card-header>
    <div slot="title">Data Grid</div>
    <div slot="subtitle">Card Example</div>
    <mlv-icon-button slot="header-action" icon-name="filter-stroke"></mlv-icon-button>
  </mlv-card-header>
  <mlv-grid container="flat">
    ...
  </mlv-grid>
</mlv-card>
```

## Card

<Canvas of={Card} />

## Card Tabs

<Canvas of={CardTabs} />

<element-api tag="mlv-grid"></element-api>

<element-api tag="mlv-grid-header"></element-api>

<element-api tag="mlv-grid-column"></element-api>

<element-api tag="mlv-grid-row"></element-api>

<element-api tag="mlv-grid-cell"></element-api>
