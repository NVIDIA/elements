import { Meta, Canvas } from '@storybook/addon-docs';
import { ColumnFixed, ColumnMultiFixed, ColumnStackFixed, ColumnDynamicFixed } from './grid.stories';

<Meta title="Elements/Data Grid/Column Fixed" />

# Data Grid Column Fixed

<element-metrics tag="mlv-grid-column"></element-metrics>

Columns positioned to a fixed location within the grid preventing the column from scrolling when the grid has horizontal scroll. Use the `position="fixed"` attribute to position the column.

```html
<mlv-grid>
  <mlv-grid-header>
    <mlv-grid-column position="fixed">Column 1</mlv-grid-column>
    <mlv-grid-column>Column 2</mlv-grid-column>
    <mlv-grid-column>Column 3</mlv-grid-column>
    <mlv-grid-column>Column 4</mlv-grid-column>
  </mlv-grid-header>
  <mlv-grid-row>
    <mlv-grid-cell>cell 1-1</mlv-grid-cell>
    <mlv-grid-cell>cell 1-2</mlv-grid-cell>
    <mlv-grid-cell>cell 1-3</mlv-grid-cell>
    <mlv-grid-cell>cell 1-4</mlv-grid-cell>
  </mlv-grid-row>
  ...
</mlv-grid>
```

## Column Fixed

<Canvas of={ColumnFixed} />

## Column Multi Fixed

Multiple Columns can fixed to any given side, however fixed columns should not span past the half way point of the grid.

<Canvas of={ColumnMultiFixed} />

## Column Stack Fixed

<Canvas of={ColumnStackFixed} />

## Column Dynamic Fixed

<Canvas of={ColumnDynamicFixed} />

<element-api tag="mlv-grid"></element-api>

<element-api tag="mlv-grid-header"></element-api>

<element-api tag="mlv-grid-column"></element-api>

<element-api tag="mlv-grid-row"></element-api>

<element-api tag="mlv-grid-cell"></element-api>
