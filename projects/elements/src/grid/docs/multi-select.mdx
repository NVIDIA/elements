import { Meta, Canvas } from '@storybook/addon-docs';
import { MultiSelect, MultiSelectBulkActions } from './grid.stories';

<Meta title="Elements/Data Grid/Multi Select" />

# Data Grid Multi Select

<element-metrics tag="mlv-grid-row"></element-metrics>

Multi Select rows use a checkbox as the first focusable item within the row. When selected
set the `selected` attribute/property on the row. This will ensure selected styles as well
as the proper `ariaSelected` state for accessibility.

```html
<mlv-grid>
  <mlv-grid-header>
    <mlv-grid-column width="max-content">
      <mlv-checkbox>
        <input type="checkbox" aria-label="select all rows">
      </mlv-checkbox>
    </mlv-grid-column>
    <mlv-grid-column>Column</mlv-grid-column>
  </mlv-grid-header>
  ...
  <mlv-grid-row selected>
    <mlv-grid-cell>
      <mlv-checkbox>
        <input type="checkbox" checked aria-label="select row 1">
      </mlv-checkbox>
    </mlv-grid-cell>
    <mlv-grid-cell>Cell</mlv-grid-cell>
    ...
  </mlv-grid-row>
  ...
</mlv-grid>
```

## Multi Select

<Canvas of={MultiSelect} />

## Multi Select Bulk Actions

When a user has actions that can be applied to multiple items, use the bulk actions component.
The bulk actions should only be visible when at least one or more rows are selected.
If the bulk actions are closed then all selected rows should be deselected.

<Canvas of={MultiSelectBulkActions} />

<element-status tag="mlv-grid"></element-status>

<element-api tag="mlv-grid"></element-api>

<element-api tag="mlv-grid-header"></element-api>

<element-api tag="mlv-grid-column"></element-api>

<element-api tag="mlv-grid-row"></element-api>

<element-api tag="mlv-grid-cell"></element-api>
