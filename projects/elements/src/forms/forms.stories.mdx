import { Meta, ArgsTable, Story, Canvas } from '@storybook/addon-docs';
import { LinkedHeader } from '../../docs/utils.mdx';
import '@elements/elements/button/define.js';

<Meta title="Foundations/Forms/Documentation" />

# Forms

The form control elements leverage the native browser inputs as its public API.
This approach allows any framework or library to use the form elements and keeps
the API simple without obfuscating the native browser APIs.

<LinkedHeader>Installation</LinkedHeader>

The form elements are split into individual sub imports/entrypoints.

```typescript
import '@elements/elements/input/define.js';
```

```html
<mlv-input>
  <label>label</label>
  <input type="text" />
  <mlv-control-message>message</mlv-control-message>
</mlv-input>
```

<LinkedHeader>Framework Integrations</LinkedHeader>

Each frontend framework typically provides it's own guidance and best practices for creating forms.
See the links below for specific integration patterns for the following frameworks:

<section mlv-layout="row gap:sm align:center">
  <mlv-button>
    <a href="./?path=/docs/integrations-angular--docs#forms" onClick="location.reload()">
      <svg width="20" height="20">
        <use xmlnsXlink="http://www.w3.org/1999/xlink" xlinkHref="#angular-svg"></use>
      </svg>
      Angular Integration
    </a>
  </mlv-button>

  <mlv-button>
    <a href="./?path=/docs/integrations-lit--docs#forms" onClick="location.reload()">
      <svg width="20" height="20">
        <use xmlnsXlink="http://www.w3.org/1999/xlink" xlinkHref="#lit-svg"></use>
      </svg>
      Lit Integration
    </a>
  </mlv-button>
</section>

# Form layouts

All form elements support the following layouts for label placement, `horizontal`, `horizontal-inline`, `vertical` and `vertical-inline`.
Vertical is the default label layout for all elements.

<LinkedHeader>Horizontal Inline</LinkedHeader>

<Canvas>
  <Story id="foundations-forms-examples--horizontal-inline" />
</Canvas>

<LinkedHeader>Horizontal</LinkedHeader>

<Canvas>
  <Story id="foundations-forms-examples--horizontal" />
</Canvas>

<LinkedHeader>Vertical</LinkedHeader>

<Canvas>
  <Story id="foundations-forms-examples--vertical" />
</Canvas>

<LinkedHeader>Vertical Inline</LinkedHeader>

<Canvas>
  <Story id="foundations-forms-examples--vertical-inline" />
</Canvas>

<LinkedHeader>Kitchen Sink</LinkedHeader>

<Canvas>
  <Story id="foundations-forms-examples--kitchen-sink" />
</Canvas>
