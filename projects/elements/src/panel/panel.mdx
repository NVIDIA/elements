import { Meta, Canvas } from '@storybook/addon-docs';
import { Default, LeftSidePanel, RightSidePanel, PanelWithTrigger, ClosablePanel, PanelWithHeader, PanelWithFullHeader, PanelWithFooter } from './panel.stories';

<Meta title="Elements/Panel/Documentation" />

# Panels

<nve-api-summary tag="nve-panel"></nve-api-summary>

Panels provide a way to make additional content available alongside the primary page layout,
in a way that is accessible and minimizable.

Panels will always be minimizable, either by closing or collapsing. The proper close/collapse icon button will display depending on the
whether or not the `closable` attribute is set. By default `closable` is false and the panel will collapse down to just an expand icon.

Additionally, `panel` can be have its `side` property set to `left` or `right` to get the proper collapse/expand icon and animation.

For additional documentation of examples and patterns of composing `panel` see the [Panel Patterns](./?path=/docs/patterns-panel--docs) doc.

## Installation

```typescript
import '@nvidia-elements/core/panel/define.js';
```

```html
<nve-panel>
  <nve-panel-content>
    Panel Content
  </nve-panel-content>
</nve-panel>
```

## Standard

<Canvas of={Default} />

## Left / Right Side Panels

<nve-api-detail tag="nve-panel" type="property" value="side"></nve-api-detail>

<Canvas of={LeftSidePanel} />

<Canvas of={RightSidePanel} />

## Collapsable Panel with External Trigger

An external trigger can be used to expand/collapse or hide/show a panel. In this example clicking the button toggles the `expanded` attribute on the `panel` between `true` and `false`

<Canvas of={PanelWithTrigger} />

## Closable Panel with External Trigger

<nve-api-detail tag="nve-panel" type="property" value="closable"></nve-api-detail>

<Canvas of={ClosablePanel} />

## Panel with Header

The `panel-header` sub-component can be slotted into `panel` to get default border styling, and additional sub slots within the header.

## Panel with Full Header

In this example `panel-header` is further slotted with elements for `slot="title"`, `slot="subtitle"`, `icon-button` with `slot="action-icon"`

<Canvas of={PanelWithFullHeader} />

## Panel with Footer

<Canvas of={PanelWithFooter} />

<nve-api-status tag="nve-panel"></nve-api-status>

<nve-api-table tag="nve-panel"></nve-api-table>

<nve-api-table tag="nve-panel-header"></nve-api-table>

<nve-api-table tag="nve-panel-content"></nve-api-table>

<nve-api-table tag="nve-panel-footer"></nve-api-table>
