import { Meta, Canvas } from '@storybook/addon-docs';
import { Default, LeftSidePanel, RightSidePanel, PanelWithTrigger, ClosablePanel, PanelWithHeader, PanelWithFullHeader, PanelWithFooter } from './panel.stories';

<Meta title="Elements/Panel/Documentation" />

# Panels

<element-metrics tag="mlv-panel"></element-metrics>

Panels provide a way to make additional content available alongside the primary page layout,
in a way that is accessible and minimizable.

The Panel element is composed of a `<mlv-panel>` container element and several sub-components with default styling applied.
The following sub-components can be used within `<mlv-panel>` to achieve standard layouts:

- `<mlv-panel-header>`
- `<mlv-panel-content>`
- `<mlv-panel-footer>`

Panels will always be minimizable, either by closing or collapsing. The proper close/collapse icon button will display depending on the
whether or not the `closable` attribute is set. By default `closable` is false and the panel will collapse down to just an expand icon.

Additionally, `<mlv-panel>` can be have it's `side` property set to `left` or `right` to get the proper collapse/expand icon and animation.

For additional documentation of examples and patterns of composing `<mlv-panel>` see the [Panel Patterns](./?path=/docs/patterns-panel--docs) doc.

## Installation

```typescript
import '@elements/elements/panel/define.js';
```

```html
<mlv-panel>
  <mlv-panel-content>
    Panel Content
  </mlv-panel-content>
</mlv-panel>
```

## Standard

<Canvas of={Default} />

## Left / Right Side Panels

<Canvas of={LeftSidePanel} />

<Canvas of={RightSidePanel} />

## Collapsable Panel with External Trigger

An external trigger can be used to expand/collapse or hide/show a panel.
In this example clicking the button toggles the `expanded` attribute on the `<mlv-panel>` between `true` and `false`

<Canvas of={PanelWithTrigger} />

## Closable Panel with External Trigger

This is the same example as above only with the `closable` attribute on the `<mlv-panel>`
set to `true`

<Canvas of={ClosablePanel} />

## Panel with Header

The `<mlv-panel-header>` sub-component can be slotted into `<mlv-panel>` to get default border styling, and additional sub slots within the header.

## Panel with Full Header

In this example `<mlv-panel-header>` is further slotted with elements for `slot="title"`, `slot="subtitle"`, `<mlv-icon-button>` with `slot="action-icon"`

<Canvas of={PanelWithFullHeader} />

## Panel with Footer

<Canvas of={PanelWithFooter} />

<element-status tag="mlv-panel"></element-status>

<element-api tag="mlv-panel"></element-api>

<element-api tag="mlv-panel-header"></element-api>

<element-api tag="mlv-panel-content"></element-api>

<element-api tag="mlv-panel-footer"></element-api>
