import { Meta } from '@storybook/addon-docs';
import { Canvas } from '@nve-internals/storybook/blocks';
import { Default, Skippable, Disabled, Flat, Inline, DisableStep, DynamicItems, DynamicStepSize, Forms, NoItemsCount } from './pagination.stories';

<Meta title="Elements/Pagination/Documentation" />

# Pagination

<nve-api-summary tag="nve-pagination"></nve-api-summary>

## Installation

```typescript
import '@nvidia-elements/core/pagination/define.js';
```

```html
<nve-pagination value="1" items="100" step="20"></nve-pagination>
```

## Standard

<Canvas of={Default} />

## Skippable

<Canvas of={Skippable} />

## Disabled

<Canvas of={Disabled} />

## Flat

<Canvas of={Flat} />

## Inline

<Canvas of={Inline} />

## Disable Step

<Canvas of={DisableStep} />

## Dynamic Items

If the upper bound of items is unknown the `last-page` event can be used to determine when
to load additional data and update the pagination with the latest total of items.

<Canvas of={DynamicItems} story={{ inline: false, height: 100 }} />

## Dynamic Step Size

When custom step is provided, the select options dynamically adapt to the step and is appended to the default option list

<Canvas of={DynamicStepSize} />

## Forms

Pagination is [form associated component](https://web.dev/more-capable-form-controls/#defining-a-form-associated-custom-element) and can be used within FormData.

<Canvas of={Forms} story={{ inline: false, height: 240 }} />

## No Provided Items Count

<Canvas of={NoItemsCount} story={{ inline: false, height: 200 }} />

<nve-api-status tag="nve-pagination"></nve-api-status>

<nve-api-table tag="nve-pagination"></nve-api-table>
