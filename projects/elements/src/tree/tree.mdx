import { Meta, Story } from '@storybook/addon-docs';
import { Canvas } from '@internals/storybook/blocks';
import { Default, Border, DynamicTree, Selectable, Highlight, Links, NodeContent, SelectableMultiple, Async, VerticalNav, Overflow } from './tree.stories';

<Meta title="Elements/Tree/Documentation" />

# Tree View

<nve-api-summary tag="nve-tree"></nve-api-summary>

## Installation

```typescript
import '@nvidia-elements/core/tree/define.js';
```

```html
<nve-tree>
  <nve-tree-node expanded>
    node 1
    <nve-tree-node>node 1-1</nve-tree-node>
    <nve-tree-node>node 1-2</nve-tree-node>
  </nve-tree-node>
  <nve-tree-node>
    node 2
    <nve-tree-node>node 2-1</nve-tree-node>
    <nve-tree-node>node 2-2</nve-tree-node>
  </nve-tree-node>
</nve-tree>
```

## Standard

<Canvas of={Default} />

## Border

<nve-api-detail tag="nve-tree" type="property" value="border"></nve-api-detail>

<Canvas of={Border} />

## Selectable

<nve-api-detail tag="nve-tree-node" type="property" value="selectable"></nve-api-detail>

<Canvas of={Selectable} />

## Selectable Multi

<nve-api-detail tag="nve-tree-node" type="property" value="selectable"></nve-api-detail>

<Canvas of={SelectableMultiple} />

## Links

<Canvas of={Links} />

## Highlight

<nve-api-detail tag="nve-tree-node" type="property" value="highlighted"></nve-api-detail>

<Canvas of={Highlight} />

## Async

If data is dynamically loaded for a node use a `progress-ring` to show loading progress.

<Canvas of={Async} />

## Vertical Nav

<Canvas of={VerticalNav} story={{ inline: false, height: '600px' }} />

## Node Content

<Canvas of={NodeContent} />

## Dynamic Tree

Using the `open` and `close` events, trees can conditionally render node based on if the node is expanded.

<Canvas of={DynamicTree} />

Example in lit-html:

```html
<nve-tree-node .expandable="${node.nodes.length}" .expanded="${node.expanded}" @open="${e => this.#open(e, node)}" @close="${e => this.#close(e, node)}">
  ${node.label} node
  ${node.expanded ? node.nodes.map(n => html`${this.#getNodeList(n)}`) : nothing}
</nve-tree-node>
```

## Overflow

If the tree expands past its container, use overflow on the parent to enable scrolling.

<Canvas of={Overflow} />

<nve-api-status tag="nve-tree"></nve-api-status>

<nve-api-table tag="nve-tree"></nve-api-table>

<nve-api-table tag="nve-tree-node"></nve-api-table>
