## Figma Output

The `./figma` directory contains the token artifacts exported from Figma. Many
value formats are not web/style-dictionary friendly, and many value references
do not work currently. These are source controlled so we can have a historical
record as the values exported change over time until the Figma Tokens format is
stabilized and better supported.
