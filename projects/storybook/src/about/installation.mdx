import { Meta } from '@storybook/addon-docs';

<Meta title="About/Installation" />

# Installation

## NPM/Artifactory

Add the following references to the `.npmrc` file in the root of your project where your `package.json` is located.

```bash
registry=https://registry.npmjs.org
```

Login to [Artifactory](https://registry.npmjs.org once logged in run the following:

```bash
npm login
```

Install base packages:

```bash
npm install @nvidia-elements/themes @nvidia-elements/styles @elements/elements --legacy-peer-deps
```

## Usage

Elements is split into multiple small packages. This allows you to choose what
tools are needed for your application and omit anything unessesary, improving
overall application performance.

```css
/* import the global CSS into your project */
@import '@elements/elements/dist/index.css';
@import '@elements/elements/dist/inter.css';
```

```javascript
// load via JavaScript imports to make available in HTML templates
import '@elements/elements/button/define.js';
import '@elements/elements/dot/define.js';
...
```

```html
<nve-button>hello there</nve-button>
```
