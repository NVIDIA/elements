import { Meta } from '@storybook/addon-docs';
import { Canvas } from '@internals/storybook/blocks';
import { LayoutHeaderFullWidthCards, LayoutHeaderGridCards, LayoutHeaderPanelCards } from './patterns.stories';

<Meta title="Patterns/Layout" />

# Layout Patterns

## Header Full Width Cards

A layout pattern with header, and full-width stacked cards.

<Canvas of={LayoutHeaderFullWidthCards} />

## Header Grid Cards

A layout pattern with header, grid cards, and right side panel.

<Canvas of={LayoutHeaderGridCards} />

## Header Panel Cards

A layout pattern with header, stacked cards, and right side panel.

<Canvas of={LayoutHeaderPanelCards} />
