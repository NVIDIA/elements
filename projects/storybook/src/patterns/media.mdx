import { Meta } from '@storybook/addon-docs';
import { Canvas } from '@internals/storybook/blocks';
import { ContentRow } from './browse.stories';
import { PageLayoutVideo, PageLayoutMultiVideo } from '../../../elements/src/page/page.stories';

<Meta title="Patterns/Media" />

# Media Patterns

## Single Video Layout

<Canvas of={PageLayoutVideo} story={{ height: '640px', padding: 0, inline: false }} />

## Multi Video Layout

<Canvas of={PageLayoutMultiVideo} story={{ height: '720px', padding: 0, inline: false }} />
