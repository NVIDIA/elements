import { Meta, Canvas } from '@storybook/addon-docs';
import { ShortcutFilled, ShortcutFlat, ShortcutDropdown } from './patterns.stories';

<Meta title="Patterns/Keyboard Shortcut" />

# Keyboard Shortcut Patterns

Use `mlv-text="code"` on a `<kbd>` element for displaying keyboard shourtcut codes.

## Standard Styling

<Canvas of={ShortcutFilled} />

## Flat Styling

<Canvas of={ShortcutFlat} />

## Keyboard Shortcut in Dropdown

<Canvas of={ShortcutDropdown} />
