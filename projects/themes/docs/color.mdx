import { Meta } from '@storybook/addon-docs';
import { Canvas } from '@nve-internals/storybook/blocks';
import { ColorPalette } from './tokens.stories';

<Meta title="Foundations/Themes/Color" />

# Color Palette

<p nve-text="body" className="full-width">
  Colors should be used via the system tokens (--nve-sys-*) when possible to ensure the appropriate value is used in a semantic and meaningful way.
</p>

<Canvas of={ColorPalette} />
