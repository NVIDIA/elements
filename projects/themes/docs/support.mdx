import { Meta, Story } from '@storybook/addon-docs';
import { Canvas } from '@internals/storybook/blocks';
import { Statuses as IconStatus } from '../../elements/src/icon/icon.stories';
import { SupportStatus as DotStatus } from '../../elements/src/dot/dot.stories';
import { Status as AlertStatus } from '../../elements/src/alert/alert.stories';
import { AlertGroupStatus as AlertGroupStatus, Prominence } from '../../elements/src/alert/alert-group.stories';
import { Status as NotificationStatus } from '../../elements/src/notification/notification.stories';
import { Status as ToastStatus } from '../../elements/src/toast/toast.stories';
import { Support, Accent } from './tokens.stories';

<Meta title="Foundations/Themes/Support" />

# Support

Support colors are used in combination of text or iconography to convey status of
an element or interation. Status type components inlcude `alerts`, `badges`, `tags` and `form control` messages.

- `default`
- `accent`
- `success`
- `warning`
- `danger`

## Icon Support

<Canvas of={IconStatus} />

## Dot Support

<Canvas of={DotStatus} />

## Alert Support

<Canvas of={AlertStatus} />

## Alert Group

<Canvas of={AlertGroupStatus} />

## Alert Group Banner

<Canvas of={Prominence} />

## Notification Snackbar

<Canvas of={NotificationStatus} story={{ inline: false, height: '500px' }} />

## Toasts

<Canvas of={ToastStatus} story={{ inline: false, height: '300px' }} />

## Support Tokens

<Story of={Support} />

## Accent Tokens

Accent tokens are used as system wide color accents. Components using accent colors
inlcude checkboxes, radios, range inputs and form controls.

<Story of={Accent} />
