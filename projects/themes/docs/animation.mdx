import { Meta, Story } from '@storybook/addon-docs';
import { Canvas } from '@internals/storybook/blocks';
import { Animation } from './tokens.stories';
import { ReducedMotion } from './theme.stories';

<Meta title="Foundations/Themes/Animation" />

# Animation

Animation tokens provide basic timings for animations and hook into theme support for reduced motion.

<Story of={Animation} />

## Reduced Motion

<Canvas of={ReducedMotion} />
