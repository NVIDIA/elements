<Meta title="About/Getting Started" />

# MagLev Elements

## Foundational building blocks for MagLev UI Patterns.

## Installation

To install the MagLev UI Elements, run the following command:

```bash
pnpm install @elements/elements
```

Once installed import the global CSS into your project.

```css
@import '@elements/elements/dist/index.css';
```

You will also need to specify the Design System font face.
```css
  @font-face {
    font-family: 'Inter var';
    src: url('./assets/Inter-roman.var.woff2') format('woff2');
    font-weight: 100 900;
    font-style: normal;
    font-display: swap;
    font-named-instance: 'Regular';
  }
```

Elements can be imported/loaded via JavaScript imports and made available to use in your HTML templates.

```javascript
import '@elements/elements/button/define.js';
```

```html
<mlv-button>hello there</mlv-button>
```

<mlv-button interaction="emphasize">hello there</mlv-button>

Alternatively, you can import the entire @elements/elements library, but will incur a larger bundle size.
```javascript
import '@elements/elements';
```

<section mlv-layout="row gap:sm align:center">
  <mlv-button>
    <a href="./?path=/story/about-angular--page">Angular Installation</a>
  </mlv-button>
  <mlv-button>
    <a href="./?path=/story/about-lit--page">Lit Installation</a>
  </mlv-button>
</section>
