import { Meta, Story } from '@storybook/addon-docs';

<Meta title="About/Getting Started" />

<Story id="internal-integration--empty" />

# MagLev Elements

## Foundational building blocks for MagLev UI.

### Installation

Configure npm to use the internal package registry.

```bash
npm --global config set @elements/elements:registry https://artifactory.build.nvidia.com/artifactory/api/npm/elements-npm/
```

To install the MagLev UI Elements, run the following command:

```bash
pnpm install @elements/elements
```

Once installed import the global CSS into your project.

```css
@import '@elements/elements/dist/index.css';
@import '@elements/elements/dist/inter.css';
```

Elements can be imported/loaded via JavaScript imports and made available to use in your HTML templates.

```javascript
import '@elements/elements/button/define.js';
```

```html
<nve-button>hello there</nve-button>
```

<nve-button interaction="emphasize">hello there</nve-button>

<hr /><br />

<section nve-layout="row gap:sm align:center">
  <nve-button>
    <a href="./?path=/story/about-angular--page">Angular Integration</a>
  </nve-button>
  <nve-button>
    <a href="./?path=/story/about-lit--page">Lit Integration</a>
  </nve-button>
  <nve-button>
    <a href="./?path=/story/about-react--page">React Integration</a>
  </nve-button>
</section>
